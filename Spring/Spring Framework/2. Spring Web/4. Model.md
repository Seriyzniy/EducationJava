Смотри подразделы:
- [[4.1 ПРАКТИКА - Возврат данных пользователю]]

***

Model - такой компонент приложения, отвечающий за работу с данными

![[Pasted image 20241107103743.png]]

> [!NOTE] Interface Model
> Model является интерфейсом
> ```java
> public interface Model {
> ```
> 
> Поэтому нельзя сделать так:
> ```java
> @GetMapping("/greeting") 
> public String showGreeting() { 
> 	Model model = new Model();  //Так НЕЛЬЗЯ
> 	
> 	model.addAttribute("message", "Hello, World!"); 
> 	return "greeting"; // Имя представления }
> ```

##### 1.1 Варианты передачи/использования интерфейса Model
- Передача в качестве аргумента (Spring сам внедрит необходимый объект)
```java
@GetMapping("/greeting") 
public String showGreeting(Model model) { 
	model.addAttribute("message", "Hello, World!"); 
	return "greeting"; // Имя представления 
}
```

- Использование класса `ModelMap`
```java
@GetMapping("/greeting") 
public String showGreeting() { 
	ModelMap model = new ModelMap();
	model.addAttribute("message", "Hello, World!"); 
	return "greeting"; // Имя представления 
}
```

- Использование класса `ModelAndView`, 
Класс ==объединяет== в себе ==модель и представление==. Его можно использовать, чтобы одновременно передать данные и указать, какое представление нужно отобразить.
```java
@GetMapping("/welcome") 
public ModelAndView welcome() { 
	ModelAndView modelAndView = new ModelAndView("welcomeView"); 
	
	modelAndView.addObject("name", "John"); 
	return modelAndView; 
}
```
# 2. Обмен данными между CONTROLLER и VIEW
![[Pasted image 20241107103957.png]]

Для получения доступа к модели из контроллера, необходимо передать в качестве параметра эту модель.

![[Pasted image 20241107104142.png]]
