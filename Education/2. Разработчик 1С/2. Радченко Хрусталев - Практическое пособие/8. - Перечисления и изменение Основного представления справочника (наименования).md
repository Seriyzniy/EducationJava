## **Зачем нужно перечисление**
У элементов справочника _Номенклатура_ нет признака, отличающего материал от услуги. 

Группировка по папкам ненадёжна (группы можно удалить или переименовать). Нужен постоянный признак, независящий от структуры справочника.


## Что такое перечисление
**Перечисление** — объект конфигурации для хранения постоянных, неизменяемых в процессе работы наборов значений (например: "Материал", "Услуга"). 

Значения задаются при разработке и не могут быть изменены пользователем. 
Алгоритмы конфигурации могут на них опираться.

---
# ПРАКТИКА
---
# 1. Добавление перечисления
Откроем конфигуратор и создадим сначала новый объект конфигурации `Перечисление` с именем `ВидыНоменклатуры`. 

На закладке `Данные` добавим два значения перечисления: `Материал` и `Услуга`. 

Для этого нажмем кнопку `Добавить` над списком значений перечисления
![[Pasted image 20260215163617.png]]

## **Привязка номенклатуры к значениям перечисления**
Чтобы каждый элемент справочника _Номенклатура_ имел признак "Материал" или "Услуга", добавим соответствующий реквизит.

### **В конфигураторе:**
1. Открыть справочник _Номенклатура_ → закладка _Данные_.
2. Кнопка _Добавить_ над списком реквизитов.
3. Имя реквизита: _ВидНоменклатуры_.
4. Тип: _ПеречислениеСсылка.ВидыНоменклатуры_.

![[Pasted image 20260215164037.png]]

Теперь у каждого элемента справочника появилось поле для выбора значения из перечисления (Материал или Услуга). Далее в режиме 1С:Предприятие нужно проставить эти значения для существующих элементов.

### В режиме "1С:Предприятие"
После этого запустим «1С:Предприятие» в режиме отладки. В режиме 1С:Предприятие зададим для каждого элемента справочника Номенклатура соответствующее значение реквизита Вид номенклатуры

![[Pasted image 20260215165121.png]]

Теперь посмотрим, как можно применить новые данные, полученные благодаря использованию перечисления ВидыНоменклатуры.

---
# 2. Произвольное представление номенклатуры
По умолчанию ссылки на номенклатуру отображаются в виде наименования (задаётся свойством справочника _Основное представление_). 

Стандартное представление номенклатуры (как и любого другого элемента справочника) определяется свойством справочника Основное представление. 

По умолчанию это свойство установлено в значение В виде наименования
![[Pasted image 20260215165321.png]]

> 🔥Мы хотим, *чтобы рядом с наименованием показывался и вид (Материал/Услуга)*.🔥

Поэтому, например, в табличной части документов в колонке Номенклатура **мы видим не ссылку на номенклатуру, а ее наименование**
![[Pasted image 20260215165359.png]]

Было бы удобно, чтобы при отображении ссылок на номенклатуру в интерфейсе «1С:Предприятия» рядом с наименованием номенклатуры показывался бы и ее вид (материал или услуга). 

Выполним это изменение.

### В режиме «Конфигуратор»
Чтобы при отображении номенклатуры к наименованию добавлялся вид (Материал/Услуга), нужно настроить два обработчика в модуле менеджера справочника. 

Для этого используются обработчики событий 
- `ОбработкаПолученияПолейПредставления` 
- и `ОбработкаПолученияПредставления` 
менеджера соответствующего объекта

**ОбработкаПолученияПолейПредставления** — определяет, какие реквизиты участвуют в формировании представления:
![[Pasted image 20260215170153.png]]

**ОбработкаПолученияПредставления** — формирует итоговую строку представления:
![[Pasted image 20260215170328.png]]
> **НРег** — это встроенная функция языка 1С, которая преобразует все символы строки в **нижний регистр** (прописные буквы). 
> 
> Название расшифровывается как 
> - «**Н**ижний **Рег**истр» 
> Также есть и противоположное действие 
> - «**В**ерхний **Рег**истр».


Прокомментируем этот код
В обработчике события `ОбработкаПолученияПолейПредставления` мы описываем, какие реквизиты будут участвовать в формировании представления документа. 

Для этого сначала мы устанавливаем параметр `СтандартнаяОбработка` в значение Ложь, а затем добавляем в массив `Поля` нужные реквизиты справочника. 
- Если параметр `Стандартная Обработка` в обработчике не установлен в значение Ложь, то массив Поля будет заполнен реквизитами, используемыми для формирования стандартного представления данного объекта (в нашем случае – это Наименование). 

В обработчике события `ОбработкаПолученияПредставления` мы описываем алгоритм получения параметра Представление из массива полей, заданных нами в предыдущем обработчике. 

Данные, необходимые для формирования представления, передаются с помощью параметра Данные типа Структура. 

Для этого сначала мы устанав ливаем параметр `СтандартнаяОбработка` в значение Ложь, а затем получаем представление номенклатуры путем добавления к ее наименованию вида номенклатуры, заключенного в скобки. Если параметр СтандартнаяОбработка в обработчике не установлен в значение Ложь, то система попытается сформировать стандартное представление объекта, исходя из переданных данных

### В режиме «1С:Предприятие»
Запустим «1С:Предприятие» в режиме отладки. В результате при открытии документа Оказание услуги в табличной части мы видим заданное нами представление номенклатуры
![[Pasted image 20260215171101.png]]

---
# 3. Регистрация расхода только той номенклатуры, которая является материалом
На шестом занятии [[5.1 -- Регистры Накоплений]] мы создали движения документа «ОказаниеУслуги» по регистру «ОстаткиМатериалов», но они некорректны: в регистр попадают не только материалы, но и услуги (листинг 10.3). 

Теперь доработаем процедуру проведения документа так, чтобы в регистре появлялись только записи о расходе материалов. 

Для этого мы сначала 
- в режиме Конфигуратор изменим процедуру проведения документа так, чтобы в регистр попадали записи только о той номенклатуре, которая является материалом, 
- а потом в режиме 1С:Предприятие заново проведем (перепроведем) все документы Оказание услуги, чтобы данные в регистре изменились в соответствии с новым алгоритмом проведения документа

Этот способ не самый эффективный, но позволит получить нужные данные. Более производительный вариант рассмотрим после изучения запросов.

## В режиме «Конфигуратор»
Скорректируем движения документа, исключив из обработки те строки табличной части, в которых находятся услуги

Для этого 
- откроем в конфигураторе модуль документа *Оказание Услуги* (контекстное меню документа – Открыть модуль объекта) 
- и добавим в обработчик события *ОбработкаПроведения* это условие. 
- Текст следует добавить в начало цикла обхода табличной части документа после строки:
```
Для Каждого ТекСтрокаПереченьНоменклатуры Из ПереченьНоменклатуры Цикл. 
```

В результате процедура *ОбработкаПроведения* должна выглядеть следующим образом
![[Pasted image 20260215172230.png]]
### **Что изменилось (листинг 10.4):**  
Внутри цикла по строкам табличной части _ПереченьНоменклатуры_ перед созданием движения добавлена проверка:

```text
Если ТекСтрокаПереченьНоменклатуры.Номенклатура.ВидНоменклатуры = Перечисления.ВидыНоменклатуры.Материал Тогда
    // создаём движение
КонецЕсли;
```

**Как работает:**
- _ТекСтрокаПереченьНоменклатуры.Номенклатура_ — ссылка на элемент справочника из текущей строки.
    
- _ВидНоменклатуры_ — реквизит этого элемента (Материал или Услуга).
    
- Сравниваем со значением _Материал_ из перечисления _ВидыНоменклатуры_.
    
- Только при совпадении создаётся движение расхода в регистр.

Теперь в регистр _ОстаткиМатериалов_ попадают только материалы. Услуги игнорируются.

## В режиме «1С:Предприятие» 
Запустим «1С:Предприятие» в режиме отладки и проверим работу процедуры проведения документа Оказание услуги. 

Откроем список документов, выполнив команду Оказание услуг в разделе Оказание услуг. 

Откроем документ Оказание услуги № 1 и внесем в него следующие изменения: 
- удалим из табличной части строку, содержащую Транзистор Philips; 
- добавим услугу – Подключение воды; 
- добавим материал – Шланг резиновый

![[Pasted image 20260215173018.png]]

> Обратите внимание, что **цены подставляются автоматически из регистра** сведений Цены.
> 
> А у нас там нет Шланга резинового, поэтому его в документ не добавляем


Нажмем кнопку `Провести` в командной панели формы документа. 

Затем выполним команду `Остатки материалов` в панели навигации формы, **чтобы перейти к записям регистра Остатки материалов, связанным с данным документом**
![[Pasted image 20260215173603.png]]
Как мы видим, в движения по регистру Остатки материалов включаются только строки, содержащие материалы. Запись про услугу Подключение воды в движения не попала.