Это 80% твоей работы — **JSON конфиги**

# 1. Архитектура Kafka Connect
Как мы уже поняли из теории, **Kafka Connect позволяет нам НЕ ПИСАТЬ вручную код Kafka Producer и Consumer.** 
(см [[4. - Kafka Connect (Интеграция с внешними системами)]])

Вместо этого, Kafka Connect **предлагает готовые решения** ==в виде коннекторов==, позволяющих:
- Читать из внешней системы и оправлять в Kafka 
	(**Source Connector**)
	
- Читать из Kafka и записывать во внешнею систему 
	(**Sink Connector**)
(см [[4.1 -- Основные компоненты Kafka Connect]])

![[Pasted image 20251201133547.png]]

Каждый из **коннекторов мы просто конфигурируем**, и указываем
- данные для подключения к БД
- куда отправлять/считывать сообщения в/из топика

---
# 2. Порядок работы 
Для разработки с использованием Kafka Connect, нам необходимо настроить:
- Источник данных (в виде БД или другого)
- Кластер Kafka
- Kafka Connect
- Конфигурацию для определенного коннектора

## БД - Postgres
==ВАЖНО==, чтобы параметр `wal_level` был `logical` 
```properties
wal_level = logical
```
для Postgres в postgres.conf 

Там доступны 3 уровня
```properties
wal_level = minimal
wal_level = replica
wal_level = logical
```

НО, по умолчанию все параметры `закомментированы` через `#`, поэтому нужно расскоментировать и изменить на `logical`

## Confluent и Debezium
Теперь мы рассмотрим последние 2 пункта. И всю настройку будем выполнять в контексте Docker
### Confluent
В случае, **если** предполагается использование Kafka Connect от **Confluent**, потребуется самостоятельно: 
- добавить плагины необходимых коннекторов в директорию, указанную в `plugin.path` 
- или задаваемую через переменную окружения `CLASSPATH`. 

Настройки воркера Kafka Connect и коннекторов определяются через конфигурационные файлы, которые передаются аргументами к команде запуска воркера. Подробнее см. в [документации](https://docs.confluent.io/current/connect/userguide.html). 

### Debezium
Здесь и далее в статье **все примеры конфигурации рассматриваются в контексте Docker-образа**, распространяемого разработчиками **Debezium**. 
Он содержит все необходимые файлы плагинов (коннекторы) и предусматривает конфигурацию Kafka Connect при помощи переменных окружения.  


> [!NOTE] `Debezium` предоставляет только `Source-коннекторы`
> Он специализируется на **CDC (Change Data Capture)** — отслеживании изменений в базах данных и публикации этих изменений в Kafka.


Весь **процесс по настройке Debeizum** в варианте с коннектором осуществляется **в ДВА ЭТАПА**. Рассмотрим каждый из них:

## 1. Настройка фреймворка Kafka Connect
Для стриминга данных в кластер Apache Kafka **во фреймворке Kafka Connect задаются** специфичные **параметры**, такие как:  
- параметры **подключения к кластеру**,
	
- **названия топиков**, в которых будет храниться непосредственно **конфигурация самого коннектора**,
	
- **имя группы**, в которой запущен коннектор (в случае использования distributed-режима).

*Официальный Docker-образ* проекта *поддерживает конфигурацию при помощи переменных окружения* — этим и воспользуемся.
```
docker pull debezium/connect
```

==МИНИМАЛЬНЫЙ набор переменных окружения==, необходимый для запуска коннектора, выглядит следующим образом:
- 🔥 `BOOTSTRAP_SERVERS=kafka-1:9092,kafka-2:9092,kafka-3:9092` — начальный список серверов кластера Kafka для получения полного списка членов кластера;
- 🔥 `OFFSET_STORAGE_TOPIC=connector-offsets` — топик для хранения позиций, на которых на данный момент находится коннектор (LSN);
- 🔥 `CONNECT_STATUS_STORAGE_TOPIC=connector-status` — топик для хранения **статуса коннектора и его ЗАДАНИЙ**;
- 🔥 `CONFIG_STORAGE_TOPIC=connector-config` — топик для хранения **данных конфигурации коннектора** и его заданий;
- 🔥 `GROUP_ID=1` — идентификатор группы воркеров, на которых может выполняться задание коннектора; необходим при использовании распределённого _(distributed)_ режима.


> [!NOTE] Создание ТОПИКОВ
> Debezium создает топики по схеме:
> ```
> <serverName>.<schemaName>.<tableName>
> ```
> 
> Например:pg-server.public.dbz_heartbeat
> 
> 
> ❗❗По учолчанию,Debezium создает **ТОПИК НА КАЖДУЮ ОТСЛЕЖИВАЕМУЮ ТАБЛИЦУ**❗❗
> 


В результате, получим такую команду запуска
```java
docker run \
  -e BOOTSTRAP_SERVERS='kafka-1:9092,kafka-2:9092,kafka-3:9092' \
  -e GROUP_ID=1 \
  -e CONFIG_STORAGE_TOPIC=my_connect_configs \
  -e OFFSET_STORAGE_TOPIC=my_connect_offsets \
  -e STATUS_STORAGE_TOPIC=my_connect_statuses \ 
  debezium/connect:1.2
```

### Примечание про формат Avro
Debezium по умолчанию пишет в JSON — удобно для тестов, но для высоких нагрузок лучше использовать **Avro** в бинарном формате, снижая нагрузку на I/O. 

Для Avro нужен **Schema Registry**. 

Переменные конвертера настраиваются через соответствующий конфиг:
```
name: CONNECT_VALUE_CONVERTER_SCHEMA_REGISTRY_URL
value: http://kafka-registry-01:8081/
name: CONNECT_KEY_CONVERTER_SCHEMA_REGISTRY_URL
value: http://kafka-registry-01:8081/
name: VALUE_CONVERTER   
value: io.confluent.connect.avro.AvroConverter
```

## 2. Настройка самого коннектора
Теперь можно перейти непосредственно к конфигурации самого коннектора, который будет читать данные из источника.

В качестве примера будем использовать Postgres

Конфигурация описывается в нотации JSON и загружается в Kafka Connect при помощи POST-запроса.

Пример конфигурации коннектера для PostgreSQL
```json
{
  "name": "pg-connector",
  "config": {
    "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
    "plugin.name": "pgoutput",
    
    "database.hostname": "127.0.0.1",
    "database.port": "5432",
    "database.user": "debezium",
    "database.password": "definitelynotpassword",
    "database.dbname" : "dbname",
    "database.server.name": "pg-dev",
    
    "table.include.list": "public.(.*)",
    
    
    "heartbeat.interval.ms": "5000",
    "slot.name": "dbname_debezium",
    "publication.name": "dbname_publication",
    
    "transforms": "AddPrefix",
    "transforms.AddPrefix.type": "org.apache.kafka.connect.transforms.RegexRouter",
    "transforms.AddPrefix.regex": "pg-dev.public.(.*)",
    "transforms.AddPrefix.replacement": "data.cdc.dbname"
  }
}
```

### Принцип работы коннектера после отправки такого конфига

Принцип работы коннектора после такой настройки довольно прост: 
- **При первом запуске** он подключается к базе, указанной в конфигурации, и запускается в режиме **initial snapshot**, отправляя в Kafka начальный набор данных, полученных с помощью условного `SELECT * FROM table_name`.
	
- После того, как инициализация будет завершена, коннектор переходит в режим чтения изменений из WAL-файлов PostgreSQL.


### Опции в конфиге

`name` — **имя коннектора**, для которого используется конфигурация, описанная ниже; 
в дальнейшем это *имя используется для работы с коннектором* (*т.е. просмотра статуса/перезапуска/обновления конфигурации*) через REST API Kafka Connect;

---
`connector.class` — **класс коннектора СУБД**, который будет использоваться конфигурируемым коннектором;

`plugin.name` — **название плагина** для логического **декодирования данных из WAL-файлов**. 
На выбор доступны 
- `wal2json`, 
- `decoderbuffs` 
- и `pgoutput`. 
Первые два требуют установки соответствующих расширений в СУБД, а `pgoutput` для PostgreSQL версии 10 и выше не требует дополнительных манипуляций;


`database.*` — **опции для подключения к БД**, 
- где `database.server.name` — ***имя инстанса PostgreSQL***, используемое ***для формирования имени топика*** в кластере Kafka;

> [!NOTE] ❗❗❗ `"database.server.name"` ❗❗❗
> ОБНОВИЛИ на `topic.prefix` начиная с Debezium 2.5


`table.include.list` — **список таблиц**, в которых мы хотим отслеживать изменения; задаётся в формате `schema.table_name`; 
- ==НЕЛЬЗЯ использовать вместе с== `table.exclude.list`;


`heartbeat.interval.ms` — **интервал** (в миллисекундах), с которым коннектор отправляет **heartbeat-сообщения в специальный топик**;

`heartbeat.action.query` — запрос, который будет выполняться при отправке каждого heartbeat-сообщения (опция появилась с версии 1.1);


`slot.name` — имя **слота репликации**, который будет использоваться коннектором;

❓`publication.name` — имя [публикации](https://www.postgresql.org/docs/12/logical-replication-publication.html) в PostgreSQL, которую использует коннектор. В случае, если её не существует, Debezium попытается её создать. 
> В случае, если у пользователя, под которым происходит подключение, **недостаточно прав для этого действия** — коннектор завершит работу с **ошибкой**;


`transforms` определяет, **как именно изменять название целевого топика**:  
- `transforms.AddPrefix.type` указывает, что будем использовать регулярные выражения;

`transforms.AddPrefix.regex` — маска, по которой переопределяется название целевого топика;

`transforms.AddPrefix.replacement` — непосредственно то, на что переопределяем.

### Подробнее про heartbeat и transforms
#### ❗❗❗ Проблема ❗❗❗
Debezium читает изменения из WAL (PostgreSQL, MySQL binlog, Oracle redo log).  

Но если вы настроили коннектор НЕ на все таблицы, **А ТОЛЬКО на «РЕДКО ОБНОВЛЯЕМЫЕ»**, может возникнуть ситуация:

- В отслеживаемых таблицах **нет изменений**.
    
- Следовательно:
    - Debezium **не видит новых транзакций**.
    - Он **не обновляет offset в Kafka**.
    - Он **не продвигает LSN в слоте репликации**.
        
- PostgreSQL, видя, что слот репликации всё ещё “в прошлом”, **не может удалить старые WAL-файлы**, иначе сломается репликация.

➡️ В итоге WAL-файлы копятся, занимают диск → возможен **OUT OF DISK**.

##### LSN (Log Sequence Number)
> [!NOTE] LSN
> LSN — это указатель на конкретное место в WAL-файлах.  

==Каждая операция изменения данных== в PostgreSQL ==получает свой LSN.==

> **Debezium читает все изменения в журнале** (затем фильтрует выбирая нужное и отправляет в топик), НО ПЕРЕДВИГАЕТ LSN ТОЛЬКО в случае КОММИТА сообщения в топик

Пример:
```sql
WAL #2: изменение USERS (LSN=100)   <- Debezium отправил в Kafka
WAL #3: 5000 изменений ORDERS
WAL #4: 7000 изменений PRODUCTS
WAL #5: 9000 изменений LOGS
```
Debezium:
- прочитал #3, #4, #5
- отфильтровал их

- событие в Kafka не отправлял
- offset не обновлял
    
- restart_lsn = 100

Postgres:
→ не может удалить WAL #2–#5, пока restart_lsn = 100.


Debezium использует LSN для:
1) **Отслеживания прогресса**
	Для того чтобы "запомнить" до какого месте была произведена вычитка данных
		
	Этот LSN записывается в Kafka-топик:
	`offsets` в Kafka Connect
	
2) **Управления очисткой WAL**
	PostgreSQL не удаляет WAL-файлы, пока есть подписчик (на слоту), который их не прочитал.
	
	Если LSN «застрял» → WAL-файлы будут копиться → диск переполнится.
	
	Поэтому Debezium делает heartbeat, чтобы LSN двигался даже при отсутствии изменений.

1) **Гарантия "exactly-once" для CDC**
	Если Debezium упал:
	- **при рестарте он читает из offset-topic** последний сохранённый LSN,
    - начинает чтение WAL _с той же позиции_,
    - события не пропустятся,
    - события не повторятся.

#### Решение: heartbeat
Debezium даёт механизм «сердцебиений»:

`heartbeat.interval.ms`
Включает и задаёт периодичность отправки heartbeat-сообщений _в Kafka_.

`heartbeat.action.query`
Указывает **SQL-команду**, которая должна выполняться в PostgreSQL при отправке heartbeat.

Это и есть та «фиктивная транзакция», которая создаёт новый LSN в WAL.

##### А что если `heartbeat.action.query` НЕ указан?
Если его нет — Debezium создаёт heartbeat-сообщения в Kafka, но **не делает вообще никаких действий в базе**.

То есть:
- событие heartbeat попадёт в Kafka  
- offset сдвинется
    
- но **LSN в логическом слоте НЕ сдвинется**
- следовательно **WAL не будет очищаться** 

➡️ **heartbeat без `heartbeat.action.query` НЕ предотвращает рост WAL.**

##### Хитрость 
Если Debezuim следит за редко-обновляемой таблицей, то можно создать "слежубную" таблицу, куда будут писаться hearbeqat сообщения
```sql
"heartbeat.action.query": "INSERT INTO dbz_heartbeat(ts) VALUES (NOW())"
```

В результате, если Users долго не обновляется, то выполнится транзакция в служебную таблицу. Debezium увидит изменения одной из интересующей таблицы и передвинит LSN

При этом в Kafka будет попадать только hearbeat сообщение (в служебный топик)

Для этого создается отдельный служебный топик
```sql
"transforms": "routeHeartbeats",
"transforms.routeHeartbeats.type": "org.apache.kafka.connect.transforms.RegexRouter",
"transforms.routeHeartbeats.regex": ".*dbz_heartbeat.*",
"transforms.routeHeartbeats.replacement": "internal.heartbeat"
```

`RegexRouter` — это **трансформация в Kafka Connect**, которая **изменяет имя топика для конкретных событий**.
- `regex` — регулярное выражение, которое ищет в имени исходного топика.
    
- `replacement` — строка, на которую будет заменено совпадение.
    

То есть:
- Все события, исходно предназначавшиеся для топика `serverName.dbname.dbz_heartbeat` (по умолчанию Debezium создаёт топики по схеме `serverName.schema.table`)
    
- Попадут в новый топик `internal.heartbeat`.

⚠️ **Важно:** `RegexRouter` не создаёт топик сам по себе. Он **просто перенаправляет события в новый топик**.

### Ограничения коннектора
Стоит рассказать о следующих особенностях/ограничениях его работы:
1. Функционал коннектора для PostgreSQL полагается на концепцию логического декодирования. Поэтому он **не отслеживает запросы на изменение структуры БД** (DDL) — соответственно, в топиках этих данных не будет.
	
2. Так как используются слоты репликации, подключение коннектора возможно _только_ к ведущему экземпляру СУБД.
	
3. Если пользователю, под которым коннектор подключается к базе данных, выданы права только на чтение, то перед первым запуском потребуется вручную создать слот репликации и публикацию в БД.

---
# 2. Использование Kafka Connect
После написания конфигурации, ее необходимо отправить по сети на сам коннектор методом POST, на прокинутый порт для контейнера (или порт по умолчанию, врод 8083)
## 1. Запуск конфигурации в коннектор
После создание конфигурационного файла, его нужно отправить по HTTP на сам коннектор:
```bash
curl -i -X POST -H "Accept:application/json" \
  -H  "Content-Type:application/json" \ 
  http://localhost:8083/connectors/ \
  -d @pg-con.json 
```

Проверяем, что загрузка прошла успешно и коннектор запустился:
```bash
$ curl -i http://localhost:8083/connectors/pg-connector/status 
HTTP/1.1 200 OK
Date: Thu, 17 Sep 2020 20:19:40 GMT
Content-Type: application/json
Content-Length: 175
Server: Jetty(9.4.20.v20190813)

{"name":"pg-connector",
 "connector":
    {
	    "state":"RUNNING",
	    "worker_id":"172.24.0.5:8083"
    },
"tasks":
[
	{
	    "id":0,
		"state":"RUNNING",
		"worker_id":"172.24.0.5:8083"
	}
],"type":"source"}
```

## 2. ОБНОВЛЕНИЕ ФАЙЛА конфигурации
Чтобы ОБНОВИТЬ файл конфигурации для определенного коннектора, нужно в ФАЙЛЕ JSON:
- убрать параметр `name`
- оставить только ВНУТРЕННОСТЬ параметра `config`
- НЕ ИЗМЕНЯТЬ `plagin.name` и `slot.name`

Имя нового файла JSON может быть любым

### Отправка нового файла конфигурации
Теперь ВМЕСТО POST используем PUT, **и ИЗМЕНЯЕМ путь**
БЫЛО:
```bash
curl -i -X POST 
-H "Accept:application/json" 
-H "Content-Type:application/json" 
http://localhost:8083/connectors 
-d @debeziumConfig.json
```

СТАЛО:
```bash
curl -i -X PUT 
-H "Accept:application/json" 
-H "Content-Type:application/json" 
http://localhost:8083/connectors/my-first-connector/config 
-d @debeziumConfigUpdate.json
```

Изменился путь! В нем мы добавляем ИМЯ коннектора, и файл config
`http://localhost:8083/connectors/my-first-connector/config `



## 2. Внесения изменений в БД
После чего, так как нам коннектор ОТСЛЕЖИВАЕТ ИЗМЕНЕНИЯ В БД через журнал, то мы теперь можем просто ИЗМЕНЯТЬ ДАННЫЕ В БД, а коннектор их подхватит и отправит в Kafka

(см чуть выше где говорится как Debezium создает топики для таблиц - `serverName.schema.table`)

---
# 3. Отказоустойчивость Debezium
Рассмотрим, что в принципе может пойти не так и что будет происходить с Debezium в каждом из случаев.  
  
Есть три варианта отказа:  
1. **Отказ Kafka Connect**. Если Connect настроен на работу в распределенном режиме, для этого необходимо нескольким воркерам задать одинаковый group.id. Тогда при отказе одного из них коннектор будет перезапущен на другом воркере и продолжит чтение с последней коммитнутой позиции в топике в Kafka.
2. **Потеря связности с Kafka-кластером**. Коннектор просто остановит чтение на позиции, которую не удалось отправить в Kafka, и будет периодически пытаться повторно отправить её, пока попытка не завершится успехом.
3. **Недоступность источника данных**. Коннектор будет производить попытки повторного подключения к источнику в соответствии с конфигурацией. По умолчанию это 16 попыток с использованием [exponential backoff](https://en.wikipedia.org/wiki/Exponential_backoff). После 16-й неудачной попытки таск будет помечен как _failed_ и потребуется его ручной перезапуск через REST-интерфейс Kafka Connect.  
    - В случае с **PostgreSQL** данные не пропадут, т.к. использование слотов репликации не даст удалить WAL-файлы, не прочитанные коннектором. В этом случае есть и обратная сторона медали: если на продолжительное время будет нарушена сетевая связность между коннектором и СУБД, есть вероятность, что место на диске закончится, а это может может привести к отказу СУБД целиком.
    - В случае с **MySQL** файлы бинлогов могут быть отротированы самой СУБД раньше, чем восстановится связность. Это приведёт к тому, что коннектор перейдёт в состояние failed, а для восстановления нормального функционирования потребуется повторный запуск в режиме initial snapshot для продолжения чтения из бинлогов.
    - Про **MongoDB**. Документация гласит: поведение коннектора в случае, если файлы журналов/oplog'а были удалены и коннектор не может продолжить чтение с той позиции, где остановился, одинаково для всех СУБД. Оно заключается в том, что коннектор перейдёт в состояние _failed_ и потребует повторного запуска в режиме _initial snapshot_.  
          
        Однако бывают исключения. Если коннектор продолжительное время находился в отключенном состоянии (или не мог достучаться до экземпляра MongoDB), а oplog за это время прошёл ротацию, то при восстановлении подключения коннектор невозмутимо продолжит читать данные с первой доступной позиции, из-за чего часть данных в Kafka **не** попадёт.