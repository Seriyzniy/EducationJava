Теперь, когда у нас есть готовый проект с успешной передачей сообщений, изменим Consumer так, что теперь он будет принимать сообщения не через метод `poll` (запрос/ответ обработка), а будет использовать `Kafka Streams`