
Допустим сценарий, что у нас при отправке сообщения в Kafka происходит еще и сохранение в файл/БД

Условно, допустим что мы сначала отправляем в kafka, а потом сохраняем в БД (хотя логичнее было бы наоборот). 
Или же допустим что мы отправляем в один топик, а затем во второй 

Если сообщение отправилось в топик, и по какой-то причине программа упала, то мы не сохраним запись в БД/файл, или же не отправим сообщение во второй топик

> Это может быть очень важно, если допустим один топик принимает запрос на зачисление денег, а другой на списывание
> (в нашем случае мы отправили запрос на списание, а запрос на зачисление не пришел)

Поэтому и нужны транзакции

---
## 1. Настройка Consumer
Чтобы Consumer считывал ТОЛЬКО ЗАКОМИЧЕННЫЕ сообщения, необходимо его специально настроить

Для этого нужно добавить свойство "Уровень изоляции"
```java
props.put(  
	ConsumerConfig.ISOLATION_LEVEL_CONFIG,  
	"read_committed"  
);
```


В итоге получим такую конфигурацию
```java
package consumer.config;  
  
import model.UserInfo;  
import org.apache.kafka.clients.consumer.ConsumerConfig;  
import org.apache.kafka.common.serialization.StringDeserializer;  
import org.springframework.context.annotation.Bean;  
import org.springframework.context.annotation.Configuration;  
import org.springframework.kafka.annotation.EnableKafka;  
import org.springframework.kafka.config.ConcurrentKafkaListenerContainerFactory;  
import org.springframework.kafka.core.ConsumerFactory;  
import org.springframework.kafka.core.DefaultKafkaConsumerFactory;  
import org.springframework.kafka.support.DefaultKafkaHeaderMapper;  
import org.springframework.kafka.support.converter.MessagingMessageConverter;  
import util.CustomUserInfoDeserializer;  
  
import java.util.HashMap;  
import java.util.Map;  
  
@Configuration  
@EnableKafka  
public class KafkaConsumerConfig {  
    @Bean  
    public ConsumerFactory<String, UserInfo> consumerFactory() {  
        Map<String, Object> props = new HashMap<String, Object>();  
        props.put(  
                ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG,  
                "localhost:29092,localhost:39092,localhost:49092"  
        );  
        props.put(  
                ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG,  
                StringDeserializer.class  
        );  
        props.put(  
                ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG,  
                CustomUserInfoDeserializer.class  
        );  
        props.put(  
                ConsumerConfig.AUTO_OFFSET_RESET_CONFIG,  
                "earliest"  
        );  
        props.put(  
                ConsumerConfig.GROUP_ID_CONFIG,  
                "custom_consumer"  
        );  
        props.put(  
                ConsumerConfig.ISOLATION_LEVEL_CONFIG,  
                "read_committed"  
        );  
        
        return new DefaultKafkaConsumerFactory<>(props);  
    }  
  
    @Bean  
    public ConcurrentKafkaListenerContainerFactory<String, UserInfo> kafkaListenerContainerFactory() {  
        ConcurrentKafkaListenerContainerFactory<String, UserInfo>  
            factory = new ConcurrentKafkaListenerContainerFactory<>();  
  
        factory.setConsumerFactory(consumerFactory());  
  
        DefaultKafkaHeaderMapper headerMapper = new DefaultKafkaHeaderMapper();  
        headerMapper.addTrustedPackages("*");  
  
        MessagingMessageConverter converter = new MessagingMessageConverter();  
        converter.setHeaderMapper(headerMapper);  
  
        factory.setRecordMessageConverter(converter);  
  
        return factory;  
    }  
}
```

### Слушатель 
Слушатель остается без изменений, только лишь добавим возможность отправки сообщения без заголовков, и чтобы Consumer не падал, сделаем заголовки необязательными
```java
package consumer.service;  
  
import model.CustomHeader;  
import model.UserInfo;  
import org.springframework.kafka.annotation.KafkaListener;  
import org.springframework.lang.Nullable;  
import org.springframework.messaging.Message;  
import org.springframework.messaging.handler.annotation.Header;  
import org.springframework.stereotype.Component;  
  
@Component  
public class ConsumerService {  
  
    @KafkaListener(  
            topics = "topic-spring-1",  
            containerFactory = "kafkaListenerContainerFactory"  
    )  
    public void listener(
	    Message<UserInfo> message,  
		@Nullable @Header("StringHeader") String simple_header,  
		@Nullable @Header("CustomHeader") CustomHeader custom_header) 
	{  
        System.out.println("[CONSUMER] In listener");  
        System.out.println(message.getPayload());  
  
        if(simple_header != null && custom_header != null) {  
            System.out.println(simple_header);  
            System.out.println(custom_header);  
        }  
    }  
}
```

---
## 2. Конфигурация Producer на использование транзакций
(см [[2. - Транзакции]] и [[2.1 -- Примеры конфигурации Транзакций]])

### Транзакционный ID для Producer в Фабрике Producer
Как мы помним, в Kafka для использования транзакций **необходимо указать для Producer, ID**, который будет идентифицировать каждого отправителя. **А также префикс для транзакционный Producer**

В Spring Kafka это делается при помощи 
```java
@Bean  
public ProducerFactory<String, UserInfo> producerFactory(){  
    Map<String, Object> props = new HashMap<>();  
    props.put(  
            ProducerConfig.BOOTSTRAP_SERVERS_CONFIG,  
            "localhost:29092,localhost:39092,localhost:49092"  
    );  
    props.put(  
            ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG,  
            StringSerializer.class  
    );  
    props.put(  
            ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG,  
            CustomUserInfoSerializer.class  
    );  
  
    // 1️⃣Добавление Producer ID для транзакций и включение идемпотентности 
    props.put(  
            ProducerConfig.TRANSACTIONAL_ID_CONFIG,  
            "transact-id-1");  
    props.put(  
            ProducerConfig.ENABLE_IDEMPOTENCE_CONFIG,  
            true);  
  
    DefaultKafkaProducerFactory<String, UserInfo> factory =  
            new DefaultKafkaProducerFactory<>(props); 
    
    // 2️⃣Добавление префикса для транзационных продюсеров 
    factory.setTransactionIdPrefix("transaction-id-prefix-");  
    return factory;  
}
```

Или же в KafkaTemplate через аналогичный метод

### Бин менеджера транзакций Spring Kafka
Далее, **необходимо создать бин менеджера транзакций Kafka**, чтобы он участвовал в управлении транзакцией при использовании аннотации @Transactional
```java
@Bean  
public KafkaTransactionManager<String, UserInfo> kafkaTransactionManager( 
        ProducerFactory<String, UserInfo> producerFactory) {  
    return new KafkaTransactionManager<>(producerFactory);  
}
```

### KafkaTemplate
KafkaTemplate без изменений, он использует ту же фабрику ProducerFactory что и KafkaTransactionManager

> Чтобы была одна и та же фабрика, важно, чтобы в конструктор передавался один и тот же бин фабрики
> 
> Для этого лучше в метод создания бина (KafkaTeplate/TransactionManager) передавать объект бина фабрики, а не вызывать метод создания бина фабрики в конструкторе 
```java
@Bean  
public KafkaTemplate<String, UserInfo> kafkaTemplate(  
        ProducerFactory<String, UserInfo> producerFactory){  
    return new KafkaTemplate<>(producerFactory);  
}
```

### Обязательная @EnableTransactionManagement
Чтобы включить использование транзакций Spring, **необходима аннотаций @EnableTransactionManagement над классом конфигурации**
```java
@Configuration  
@ComponentScan({"producer.config", "producer.service"})  
@EnableTransactionManagement  
public class KafkaProducerApp {  
    public static void main(String[] args) {  
        ApplicationContext context =  
                new AnnotationConfigApplicationContext(KafkaProducerApp.class);  
  
        ProducerService producerService = (ProducerService)context.getBean("producerService");  
        producerService.produceMessage();  
    }  
}
```


В результате получим такую конфигурацию
```java
package producer.config;  
  
import model.UserInfo;  
import org.apache.kafka.clients.producer.ProducerConfig;  
import org.apache.kafka.common.serialization.StringSerializer;  
import org.springframework.context.annotation.Bean;  
import org.springframework.context.annotation.Configuration;  
import org.springframework.kafka.core.DefaultKafkaProducerFactory;  
import org.springframework.kafka.core.KafkaTemplate;  
import org.springframework.kafka.core.ProducerFactory;  
import org.springframework.kafka.transaction.KafkaTransactionManager;  
import util.CustomUserInfoSerializer;  
  
import java.util.HashMap;  
import java.util.Map;  
  
@Configuration  
public class KafkaProducerConfig {  
    @Bean  
    public ProducerFactory<String, UserInfo> producerFactory(){  
        Map<String, Object> props = new HashMap<>();  
        props.put(  
			ProducerConfig.BOOTSTRAP_SERVERS_CONFIG,  
			"localhost:29092,localhost:39092,localhost:49092"  
        );  
        props.put(  
			ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG,  
			StringSerializer.class  
        );  
        props.put(  
			ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG,  
			CustomUserInfoSerializer.class  
        );  
  
        // Добавление транзакций  
        props.put(  
			ProducerConfig.TRANSACTIONAL_ID_CONFIG,  
			"transact-id-1");  
        props.put(  
			ProducerConfig.ENABLE_IDEMPOTENCE_CONFIG,  
			true);  
  
        DefaultKafkaProducerFactory<String, UserInfo> factory =  
                new DefaultKafkaProducerFactory<>(props); 
                 
        factory.setTransactionIdPrefix("transaction-id-prefix-");  
        return factory;  
    }  
  
    @Bean  
    public KafkaTemplate<String, UserInfo> kafkaTemplate(  
            ProducerFactory<String, UserInfo> producerFactory){  
        return new KafkaTemplate<>(producerFactory);  
    }  
  
    @Bean  
    public KafkaTransactionManager<String, UserInfo> kafkaTransactionManager(  
            ProducerFactory<String, UserInfo> producerFactory) {  
        return new KafkaTransactionManager<>(producerFactory);  
    }  
}
```

## 2. Использование ЛОКАЛЬНОЙ транзакции в ProducerService
Как уже говорилось в [[2. - Транзакции]] есть 4 способа использования транзакций

Самый простой это использование локальной транзакции, которая следит только за выполнение транзакций в Kafka

Для этого используется метод
```java
kafkaTemplate.executeInTransaction()
```

```java
package producer.service;  
  
import model.CustomHeader;  
import model.UserInfo;  
import org.springframework.kafka.core.KafkaTemplate;  
import org.springframework.kafka.support.KafkaHeaders;  
import org.springframework.messaging.Message;  
import org.springframework.messaging.support.MessageBuilder;  
import org.springframework.stereotype.Component;  
  
import java.util.UUID;  
import java.util.concurrent.TimeUnit;  
  
@Component  
public class ProducerService {  
    private final KafkaTemplate<String, UserInfo> kafkaTemplate;  
  
    public ProducerService(KafkaTemplate<String, UserInfo> kafkaTemplate) {  
        this.kafkaTemplate = kafkaTemplate;  
    }  
  
    public void produceMessage(){  
        System.out.println("[PRODUCER] In produceMessage");  
  
        kafkaTemplate.executeInTransaction(kt -> {  
            try {  
                Message<UserInfo> message = MessageBuilder  
					.withPayload(
						new UserInfo(
							"Tom in Transaction",
							100))  
					.setHeader(
						KafkaHeaders.TOPIC, 
						"topic-spring-1")  
					.setHeader(
						"StringHeader", 
						"string")  
					.setHeader(
						"CustomHeader", 
						new CustomHeader(
							"transactionHeader", 
							UUID.randomUUID(), 
							17))  
					.build();  
  
                kt.send(message).get(10, TimeUnit.SECONDS);  
  
            } catch (Exception e) {  
                System.out.println(
                "Exception on send Message in transaction");  
            }  
  
			// Симуляция прерывания транзакции
            int y = 1 / 0;  
  
            return null;  
        });  
    }  
}
```

> [!NOTE] ВАЖНО
> ВАЖНО, чтобы симуляция ошибки НЕ ОБРАБАТЫВАЛАСЬ внутри блока кода транзакции, иначе менеджер транзакций подумает что ошибка была обработана, и поэтому транзакцию можно коммитить
> 
> ❗❗❗ОШИБКА, из-за которой транзакция может упасть, ДОЛЖНА ПРОКИДЫВАТЬСЯ НАВЕРХ, а не перехватываться в cath()❗❗❗

## 3. Запуск
### Запуск Producer
Теперь запустим программу, и посмотрим логи
![[Pasted image 20251126224043.png]]

Если проматать логи чуть правее, то увидим лог, о том, что транзакция была откатана
![[Pasted image 20251126224155.png]]

### Запуск Consumer
На самом деле COnsumer был запущен до запуска Producer, чтобы не делать несколько скринов, сделаем один скрин вывода COnsumer
![[Pasted image 20251126224256.png]]

Как видим, сообщение не пришло

### Kafka-ui
Теперь посмотрим Kafka-ui, и убедимся, что сообщение лежит в топике
![[Pasted image 20251126224401.png]]

Как видим, сообщение лежит в топике

### Запуск Producer без ошибки
Теперь закомментируем ошибку деления на 0, и запустим Producer. Так как Producer создан с настройкой идемпотентности, то при отправке этого же сообщения, оно не будет сохранено в Kafka, и будет лишь выполнен коммит сообщения
![[Pasted image 20251126224648.png]]

### Просмотр COnsumer
![[Pasted image 20251126224712.png]]

Сообщение было успешно прочитано!


## 4. Использование @Transactional в методе отправки
Теперь провернем то же самое, только с использованием @Transactional.

Вместо этого, просто убираем блок кода с `executeInTransaction()`, и на метод отправки вешаем аннотацию @Transactional

```java
package producer.service;  
  
import model.CustomHeader;  
import model.UserInfo;  
import org.springframework.kafka.core.KafkaTemplate;  
import org.springframework.kafka.support.KafkaHeaders;  
import org.springframework.messaging.Message;  
import org.springframework.messaging.support.MessageBuilder;  
import org.springframework.stereotype.Component;  
import org.springframework.transaction.annotation.Transactional;  
  
import java.util.UUID;  
import java.util.concurrent.TimeUnit;  
  
@Component  
public class ProducerService {  
    private final KafkaTemplate<String, UserInfo> kafkaTemplate;  
  
    public ProducerService(KafkaTemplate<String, UserInfo> kafkaTemplate) {  
        this.kafkaTemplate = kafkaTemplate;  
    }  
  
    @Transactional  
    public void produceMessage(){  
        System.out.println("[PRODUCER] In produceMessage");  
  
        try {  
            Message<UserInfo> message = MessageBuilder  
				.withPayload(new UserInfo(
					"Tom in Transaction with Annotaion", 
					100))  
				.setHeader(KafkaHeaders.TOPIC, "topic-spring-1")  
				.setHeader("StringHeader", "string")  
				.setHeader(
					"CustomHeader", 
					new CustomHeader(
						"transactionHeader", 
						UUID.randomUUID(), 
						17))  
				.build();  
  
            kafkaTemplate.send(message).get(10, TimeUnit.SECONDS);  
  
        } catch (Exception e) {  
            System.out.println(
            "Exception on send Message in transaction");  
        }  
  
        int y = 1 / 0;  
    }  
}
```

> [!NOTE] ВАЖНО
> ОШИБКА, из-за которой транзакция может упасть, ДОЛЖНА ПРОКИДЫВАТЬСЯ НАВЕРХ, а не перехватываться в cath()

## 5. Запуск
![[Pasted image 20251126225350.png]]


Как видно, поведение аналогично, но @Transactional можно привязать с другими транзакциями (например к БД), и тогда при ошибке в БД, сообщение в Kafka не закомитится