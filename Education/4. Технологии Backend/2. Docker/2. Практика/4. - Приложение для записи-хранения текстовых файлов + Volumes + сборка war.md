
Напишем приложение на сервлетах, которое будет считывать данные по Http, записывать/считывать в файл. А сохранять оно будет по пути, указанному в переменную окружения SAVE_FILE_PATH

> [!NOTE] ⭐⭐⭐⭐ СТРУКТУРА `WAR` файла ОБЯЗАТЕЛЬНА ⭐⭐⭐⭐
> Перед началом, НЕОБХОДИМО ПОВТОРИТЬ структуру ФАЙЛА `WAR`, а именно
> ```css
> myapp.war
│
├── WEB-INF/
│   ├── web.xml              ← обязательно здесь!
│   ├── classes/
│   │   └── com/example/
│   │       └── MyServlet.class
│   └── lib/
│       └── (тут твои .jar-зависимости, если есть)
> ```

ФАЙЛЫ С ИСХОДНЫМ КОДОМ будем хранить в ==ПАКЕТЕ== (Не папке) `main.java`

Для правильной структуры `WAR`-файла, создадим НЕОБХОДИМУЮ ПАПКУ `WEB-INF` внутри проекта. 
ВНУТРИ нее, НУЖНО, описать ПАКЕТ, с скомпилированным файлом

В итоге получим такую структуру:
![[Pasted image 20251023095849.png]]
# 📕 1. Приложение на сервлетах
Для начала напишем сервлет, который будет записывать/считывать в файл данные

> [!NOTE] ❗❗❗НЕ ЗАБУДЬ добавить `servlet-api.jar` в `Classpath` проекта❗❗❗
> см [[3. - Простой сервлет]]

```java
package main.java.servlets;

import java.io.BufferedReader;
import java.io.File;
import java.io.FileNotFoundException;
import java.io.FileReader;
import java.io.IOException;
import java.io.PrintWriter;

import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;

public class FileManagerServlet extends HttpServlet{
	private String pathToSave;
	private final String fileName = "yourFile";
	
	public FileManagerServlet(){
		System.out.println("\n[INFO] In CONSTRUCTOR Servlet \n");
		try {
			pathToSave = System.getenv("SAVE_FILE_PATH");
			
		} catch (Exception e){
			System.out.println("[Exception]: Exception on read ENV");
		}
	}
	
	@Override
	protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
		System.out.println("\n[INFO] In POST method Servlet \n");
		try(BufferedReader requestReader = req.getReader();
			PrintWriter fileWriter = new PrintWriter(new File(pathToSave + fileName)))
		{
			String line;
			while((line = requestReader.readLine()) != null) {
				fileWriter.write(line);
			}
		} catch(FileNotFoundException e) {
			System.out.println("[Exception] Exception on OPNE file by path:" + pathToSave+fileName);
		}
	}
	
	@Override
	protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
		System.out.println("\n[INFO] In GET method Servlet \n");
		try(BufferedReader fileReader = new BufferedReader(new FileReader(pathToSave + fileName));
			PrintWriter responseWriter = resp.getWriter())
		{
			String line;
			while((line = fileReader.readLine()) != null) {
				responseWriter.write(line);
			}
		} 
	}
}
```

## 📙 Компиляция сервлета
```bash
..\JavaForDocker\src>

javac -cp "D:/Education_Java/JavaProgramming/ToolsAndConfig/ApacheServers/apache-tomcat-10.1/apache-tomcat-10.1.28/lib/servlet-api.jar" main\java\servlets\FileManagerServlet.java
```

## 📙 Добавление переменной окружения
![[Pasted image 20251022190403.png]]
## 📙Описание web.xml
Теперь опишем файл `web.xml` который читает этот файл для конфигурации сервлетов

> [!NOTE] Скопировал шапку с оригинального web.xml и добавил конфиг сервлета
>Шапку xml не надо, иначе невнятные ошибки
```xml
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                      https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
  version="6.0"
  metadata-complete="true">
  
	<servlet>
	    <servlet-name>FileManagerServlet</servlet-name>
	    <servlet-class>main.java.servlets.FileManagerServlet</servlet-class>
	</servlet>
	
	<servlet-mapping>
	    <servlet-name>FileManagerServlet</servlet-name>
	    <url-pattern>/file-manager</url-pattern>
	</servlet-mapping>
</web-app>
```

## 📙Микротест серлета
### Перемещение файла сервлета
Теперь протестируем сервлет
Для этого поместим СКОМПИЛРОВАННЫЙ файл 
в `\webapps\examples\WEB-INF\classes\пакет\файл.class`

> [!NOTE] ❗❗❗ НЕ ЗАБУДЬ повторить структуру пакетов класса сервлетов ❗❗❗
> Добавь папку с названием пакета 

### Запуск сервера Tomcat
Запустим `startup.bat`
![[Pasted image 20251022190307.png]]

### Отправка данных
![[Pasted image 20251022191001.png]]

Проверка что файл записался
![[Pasted image 20251022191045.png]]

Чтение данных
![[Pasted image 20251022191111.png]]

---
# 📕 2. Сборка проекта в WAR файл

## 📙❗❗ОБЯЗАТЕЛЬНО❗❗, переместить в WEB-INF\classes\main\java\servelts
```bash
JavaForDocker\src>

copy main\java\servlets\FileManagerServlet.class WEB-INF\classes\main\java\servlets
```

## Сборка WAR-файла
Теперь, когда у нас есть:
- скомпилированный файл класса сервлета `FileManagerServlet.class`
- файл конфигурации сервера Tomcat `web.xml`


Можно собрать архив приложения WAR (см [[5. - Формат .WAR (Web-Application Archive)]])

![[Pasted image 20251023100852.png]]


> [!NOTE] ⭐⭐⭐⭐Для сборки WAR файла с правильной структорой (которую мы задали в проекте `WEB-INF\classes\...`) - НЕОБХОДЛИМО, чтобы эта папка `WEB-INF` попала в архив `WAR`⭐⭐⭐⭐
> - Для этого НЕОБХОДИМО указать нашу папку `WEB-INF` ==в качестве целевой== (откуда будут добавляться файлы в архив). 
> - А в качестве ==исходной папки==, использовать ==текущую== `.` (==src==), в которой лежит сама папка `WEB-INF`
> 

```bash
JavaForDocker\src>

jar -c -f JavaServlets.war -C . WEB-INF
```

На выходе получи файл `.war`
```bash
JavaForDocker\src> jar -tf JavaServlets.war
META-INF/
META-INF/MANIFEST.MF
WEB-INF/
WEB-INF/classes/
WEB-INF/classes/main/
WEB-INF/classes/main/java/
WEB-INF/classes/main/java/servlets/
WEB-INF/classes/main/java/servlets/FileManagerServlet.class
WEB-INF/web.xml
```

> [!NOTE] Как видим, файлы `FileManageServlet.class` и `web.xml` ЛЕЖАТ ПРАВИЛЬНО
> 

---
# 📕 3. Описание Dockerfile
Теперь опишем файл Dockerfile, для сборки образа приложения

![[Pasted image 20251023101423.png]]

Так как приложение ДОЛЖНО РАБОТАТЬ НА Tomcat, то в качестве БАЗОВОГО слова возьмем Tomcat, версии 10+ (т.к при написании программы использовался API Jakarta)

Так как и приложение локально запускалось на Tomcat версии 10.1.28, и Java 17, то и контейнер выберем такой же

```dockerfile
FROM tomcat:10.1.28-jdk17-temurin

RUN rm -rf /usr/local/tomcat/webapps/*

COPY JavaServlets.war /usr/local/tomcat/webapps/JavaServlets.war
```

> [!NOTE] ⭐`RUN rm -rf /usr/local/tomcat/webapps/*` ⭐
> Команда удаляет ВСЕ ПРИЛОЖЕНИЯ по умолчанию. Можно обойтись и без нее, но для чистоты выполним и ее

---
# 📕 4. Сборка образа и запуск
## Сборка Dockerfile в Image
Соберем на основе Dockerfile образ
```bash
JavaForDocker\src> docker build -t java-servlets-image:latest .
```

![[Pasted image 20251023101516.png]]


## Запуск контейнера на основе Image
Так как для работы приложения необходима ПЕРЕМЕННАЯ ОКРУЖЕНИЯ `SAVE_FILE_PATH`, то укажем ее при запуске (==УКАЗАТЬ СУЩЕСТВУЮЩУЮ ДИРЕКТОРИЮ==) 
А также укажем серверный порт для обращения к контейнеру

```bash
docker run --env SAVE_FILE_PATH=/tmp/ -p 7070:8080 --name java-servlets-container java-servlets-image
```
![[Pasted image 20251023102558.png]]

## Отправка сообщения
Теперь отправим сообщение
![[Pasted image 20251023102536.png]]

Получили вывод внутри контейнера, СОБЩЕНИЕ ПРИШЛО
![[Pasted image 20251023102618.png]]

Теперь прочитаем файл в который это сохранилось
Мы указали директорию для сохранения `/temp/`
```bash
docker exec -it java-servlets-container bash

root@85f3fbf0ee4d:/usr/local/tomcat# cd /
root@85f3fbf0ee4d:/# cd tmp
root@85f3fbf0ee4d:/tmp# cat yourFile
```

![[Pasted image 20251023102924.png]]

## Чтение сообщения через бразуер
![[Pasted image 20251023103008.png]]

---
# 📕 3. Использование volumes

Однако, если мы удалим контейнер, и пересоберем его, то файл УДАЛИТСЯ.

Для этого воспользуемся Docekr Volumes

```bash
-v D:\volum_docker:/tmp
```
С помощью ключа `-v` укажем, что все файлы который попаду в `/tmp`, должны сохраниться в папке `D:\volum_docker` (сама создастся)

```bash
docker run -v D:\volum_docker:/tmp --env SAVE_FILE_PATH=/tmp/ -p 7070:8080 --name java-servlets-container java-servlets-image
```

## Отправка файла
![[Pasted image 20251023103545.png]]

## Просмотр D:\volum_docker
![[Pasted image 20251023103622.png]]

## Теперь удалим контейнер, и создадим его занаво, и попробуем прочитать только что записанный файл
```bash
docker run -v D:\volum_docker:/tmp --env SAVE_FILE_PATH=/tmp/ -p 7070:8080 --name java-servlets-container java-servlets-image
```

![[Pasted image 20251023104046.png]]

Как видим, данные успешно считаны
![[Pasted image 20251023104054.png]]