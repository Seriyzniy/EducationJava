(СМ различие версий в 2.Настройка аутентификации)

После создания простого Spring Security приложения, которое фильтрует все запросы к серверу и перенаправляет на страницу регистрации, добавим аутентификацию

(Перед этим создали таблицу в БД Persons, настроили JdbcTemplate, PersonService, PersonDTO)
```sql
CREATE TABLE Persons(
	id int PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	p_name varchar(20),
	p_pass varchar(8),
	p_role varchar(20)
);

INSERT INTO persons(p_name, p_pass, p_role) 
VALUES('test1', 'test1', 'ROLE_USER')

```
![[Pasted image 20250223114715.png]]

# 1. Создание фильтра
Как и говорилось, Spring Security работает на фильтрах, поэтому добавим фильтр безопасности в классе конфигурации DispathcerServlet
```java
public class ServerConfig extends AbstractAnnotationConfigDispatcherServletInitializer {

	@Override
	protected Class<?>[] getRootConfigClasses() {
		return new Class[] {SecurityConfig.class, JdbcConfig.class};
	}

	@Override
	protected Class<?>[] getServletConfigClasses() {
		// TODO Auto-generated method stub
		return new Class[] { MvcConfig.class};
	}

	@Override
	protected String[] getServletMappings() {
		// TODO Auto-generated method stub
		return new String[] { "/" };
	}

	//Добавление фильтра
	 private void registerSecurityFilter(ServletContext servletContext) {
		 servletContext.addFilter("springSecurityFilterChain", new
			 DelegatingFilterProxy("springSecurityFilterChain"))
			 .addMappingForUrlPatterns(null, true, "/*"); 
	 }
	 
	//Добавление фильтра в запуск
	@Override
	public void onStartup(ServletContext servletContext) throws ServletException {
		super.onStartup(servletContext);
		registerSecurityFilter(servletContext);
	}
}
```

***
# 2. Конфигурация фильтра
Настройка аутентификации происходит в классе конфигурации SpringSecurity.

> [!NOTE] Версии
> В последних версиях Spring Security (начиная с **5.7** и выше) класс `WebSecurityConfigurerAdapter` **удален**, так как теперь используется **компонентный подход** с `SecurityFilterChain`.

```java
@Configuration
@EnableWebSecurity
@ComponentScan(value = "springSecurity.security")
public class SecurityConfig {
	private final PersonSecDetailsService detailsService;
	
	@Autowired
	public SecurityConfig(PersonSecDetailsService detailsService) {
		this.detailsService = detailsService;
	}
	@Bean
	public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
	    http
	    	.csrf(csrf -> csrf.disable())
	        .authorizeHttpRequests(auth -> auth
	            .anyRequest().authenticated()
	        )
	        .userDetailsService(detailsService)
	        .formLogin(login -> login
	            .loginPage("/login")
	            .defaultSuccessUrl("/hello", true)
	            .permitAll()
	        )
	        .logout(logout -> logout
	            .permitAll() 
	        );
	    return http.build();
	}
	
	@Bean
	public PasswordEncoder passwordEncoder() {
		return NoOpPasswordEncoder.getInstance();
	}
}
```

Здесь мы создаем два ВАЖНЫХ бина.
### SecurityFilterChain
```java
http
	.csrf(csrf -> csrf.disable())
	.authorizeHttpRequests(auth -> auth
	    .anyRequest().authenticated()
	)
	.userDetailsService(detailsService)
	.formLogin(login -> login
	    .loginPage("/login")
	    .defaultSuccessUrl("/hello", true)
	    .permitAll()
	)
	.logout(logout -> logout
	    .permitAll() 
	);
```

✔Здесь мы указываем то отключаем защиту от CSRF-атак, говорим, что ЛЮБОЙ ЗАПРОС требует аутентификации.
✔Далее указываем какой `UserDetailsService` использовать для получения данных пользователя
✔Далее идет кастомная настройка страницы входа, что делать после успешного входа
✔И наконец что выход доступен всем

### PasswordEncoder
Данный бин необходим для описания правила того, как сравнивать пароли
```java
@Bean
public PasswordEncoder passwordEncoder() {
	return NoOpPasswordEncoder.getInstance();
}
```
Здесь мы указываем что пароли хранятся в открытом виде, а не в зашифрованном

***
# 3. Контроллер возвращающий страницу входа
```java
@Controller
@RequestMapping("/login")
public class LoginController {
	@GetMapping
	public String login() {
		return "login";
	}
}
```

Т.к в конфигурации фильтра указали, что страницу входа можно получить по адресу `/login`, то необходимо создать контроллер на этом адресе, который возвращает страницу

Также можно было создать такой контроллер проще, в классе конфигурации контекста
```java
@Override // Обязательно
public void addViewControllers(ViewControllerRegistry registry) {
    registry.addViewController("/login").setViewName("login");
}
```
***
# 4. Реализация `UserDetailsService`

Теперь реализуем интерфейс UserDetailsService, который будет доставать пользователя по его имени из БД
> [!NOTE] Класс `UserDetailsService`
> Spring Security использует `UserDetailsService` для получения данных пользователя.

```java
@Service
public class PersonSecDetailsService implements UserDetailsService{
	private final PersonDAO dao;
	
	@Autowired
	public PersonSecDetailsService(PersonDAO dao) {
		this.dao = dao;
	}

	@Override
	public UserDetails loadUserByUsername(String username) throws UsernameNotFoundException {
		try {
			Person person = dao.getPersonByName(username);
			System.out.println(person.getName() + "\t" + person.getPass() + "\t" + "From detailsService");
			return new PersonDetails(person);
		}catch(EmptyResultDataAccessException e) {
			throw new UsernameNotFoundException("User not found!",e );
		}
	}
}
```

Здесь мы используем DAO а не репозиторий JPA

После получения пользователя из БД (если он есть), мы возвращаем объект `UserDetails`, который уже и подлежит обработке SpringSecurity

***
# 5. Создание класса-обёртки, содержащего сведения о безопасности класса-сущности

Для того, чтобы Spring Security мог проводить аутентификацию/авторизацию, необходимо создать класс-обёртку, в котором будет хранится сведения безопасности (пароль/логин/блокировка учетной записи и т.д)

> [!NOTE] Title
> Он определяет основные методы, которые позволяют системе безопасности получать информацию о пользователе.

Для начала создадим обычный класс-сущность, который будет храниться в БД.
```java
public class PersonDetails implements UserDetails{
	private final Person person;
	
	public PersonDetails(Person person) {
		this.person = person;
	}
	
	@Override
	public Collection<? extends GrantedAuthority> getAuthorities() {
		// TODO Auto-generated method stub
		return null;
	}

	@Override
	public String getPassword() {
		return person.getPass();
	}

	@Override
	public String getUsername() {
		return person.getName();
	}

	@Override
	public boolean isAccountNonExpired() {
		// TODO Auto-generated method stub
		return true;
	}

	@Override
	public boolean isAccountNonLocked() {
		// TODO Auto-generated method stub
		return true;
	}

	@Override
	public boolean isCredentialsNonExpired() {
		// TODO Auto-generated method stub
		return true;
	}

	@Override
	public boolean isEnabled() {
		// TODO Auto-generated method stub
		return true;
	}
}
```

### Основные методы интерфейса `UserDetails`:
1. **`getAuthorities()`** – возвращает список ролей и прав пользователя (`Collection<? extends GrantedAuthority>`).
2. **`getPassword()`** – возвращает пароль пользователя.
3. **`getUsername()`** – возвращает имя пользователя (обычно это логин или email).
4. **`isAccountNonExpired()`** – указывает, не истек ли срок действия аккаунта.
5. **`isAccountNonLocked()`** – указывает, не заблокирован ли аккаунт.
6. **`isCredentialsNonExpired()`** – указывает, не истек ли срок действия учетных данных (пароля).
7. **`isEnabled()`** – указывает, активирован ли пользователь.

***

# 5. Создание страницы регистрации/входа
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f3f3f3;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .login-container {
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            width: 300px;
        }

        h2 {
            margin-bottom: 20px;
        }

        input {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
        }

        .btn {
            width: 100%;
            padding: 10px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
        }

        .btn-login {
            background-color: #007bff;
            color: white;
            margin-bottom: 10px;
        }

        .btn-register {
            background-color: #28a745;
            color: white;
        }

        .btn:hover {
            opacity: 0.9;
        }
    </style>
</head>
<body>

    <div class="login-container">
        <h2>Please sign in</h2>
        <form id="authForm" method="POST">
            <input type="text" name="username" placeholder="Username" required>
            <input type="password" name="password" placeholder="Password" required>
            <button type="submit" class="btn btn-login" onclick="setFormAction('/sec/login')">Sign in</button>
            <button type="submit" class="btn btn-register" onclick="setFormAction('/sec/register')">Register</button>
        </form>
    </div>

    <script>
        function setFormAction(action) {
            document.getElementById('authForm').action = action;
        }
    </script>

</body>
</html>
```

Данные с этой страницы передаются в POST-запросе, с параметрами `username` и `password` на адрес по умолчанию `/login`

***
# 6. Вспомогательный контроллер, для принятия запросов на регистрацию
```java
@Controller
@RequestMapping("/register")
public class RegisterController {
	@PostMapping
	public String registerUser(@RequestParam("username") String username, @RequestParam("password") String pass) {
		System.out.println(username + "\n" + pass);
		return "redirect:/login";
	}
}
```

Здесь можно также добавить сервис, который будет сохранять пользователя в БД

# 7. Результат
При переходе по адресу http://localhost:8080/sec/hello пользователя перебрасывает на адрес /sec/login

![[Pasted image 20250301200658.png]]

При нажатии на кнопку Sign In пользователь попадает на страницу, указанную в качестве страницы по умолчанию в классе конфигураторе Spring Security (фильтр)
![[Pasted image 20250301200809.png]]

