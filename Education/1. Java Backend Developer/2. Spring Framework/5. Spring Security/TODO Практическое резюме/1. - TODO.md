## Ручное добавление filterChain в контекст сервлетов
Рассказать про разницу работы Security в Boot и просто Spring приложении
		То что в обычном необходимо вручную добавлять `securityFilterChain` как фильтр в контекст сервлетов
		[Architecture :: Spring Security](https://docs.spring.io/spring-security/reference/servlet/architecture.html#servlet-delegatingfilterproxy)

---
## Контекст Secutiry и MVC
Spring Security и MVC должны находиться в одном контексте, ЕСЛИ в Security используются конструкции `.requestMatchers("/eventapp/hello/**").permitAll()` для указания разграничения аутентификации

#### Решение 1 - добавить в один контекст
- По умолчанию `requestMatchers(String pattern)` в Spring Security 6 **использует `MvcRequestMatcher`**, который требует `HandlerMappingIntrospector`.
    
- Этот бин находится **в контексте MVC**, но сейчас у тебя **SecurityConfig находится в root context**, а `WebConfig` с MVC в servlet context.
    
- Root context **не видит бины MVC**, поэтому `mvcHandlerMappingIntrospector` отсутствует → ошибка.

#### Решение 2 - использовать другой Mathcers
Иначе использовать другой Mathcers () - использовать **AntPathRequestMatcher** вместо MvcRequestMatcher

### Верный конфиг - общий контекст
```java
package eventapp.config.tomcat;  
  
import eventapp.EventAppApplication;  
import eventapp.config.security.SecurityConfig;  
import jakarta.servlet.ServletContext;  
import jakarta.servlet.ServletException;  
import org.springframework.web.filter.DelegatingFilterProxy;  
import org.springframework.web.servlet.support.AbstractAnnotationConfigDispatcherServletInitializer;  
  
public class TomcatServerConfig extends AbstractAnnotationConfigDispatcherServletInitializer {  
    @Override  
    protected Class<?>[] getRootConfigClasses() {  
        return new Class[0];  
    }  
  
    @Override  
    protected Class<?>[] getServletConfigClasses() {  
        return new Class[]{
	        EventAppApplication.class, 
	        SecurityConfig.class};  
    }  
  
    @Override  
    protected String[] getServletMappings() {  
        return new String[]{"/"};  
    }  
  
    //Добавление фильтра  
    private void registerSecurityFilter(ServletContext servletContext) {  
        servletContext.addFilter(
	        "springSecurityFilterChain", 
	        new DelegatingFilterProxy("springSecurityFilterChain")
	    )  
        .addMappingForUrlPatterns(null, true, "/*");  
    }  
  
    //Добавление фильтра в запуск  
    @Override  
    public void onStartup(ServletContext servletContext) throws ServletException {  
        super.onStartup(servletContext);  
        registerSecurityFilter(servletContext);  
    }  
}
```

## Работа с JWT
Для работы с JWT необходимо указывать uri сервиса, с помощью которого можно будет проверять токены
