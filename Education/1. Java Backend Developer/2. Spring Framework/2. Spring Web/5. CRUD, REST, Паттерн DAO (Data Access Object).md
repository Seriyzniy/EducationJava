Смотри подразделы:
- [[5.1 ПРАКТИКА - CRUD, метод READ]]
- [[5.2 @ModelAttribute. HTML-формы]]
- [[5.3 ПРАКТИКА - CRUD, метод CREATE]]
- [[5.4 HTTP-запросы и HTML-5]]
- [[5.5 ПРАКТИКА - CRUD, методы UPDATE, DELETE (Фильтр)]]

***
# 1. CRUD

> [!NOTE] CRUD
> Стандартная классификация функций по манипуляциям с `данными в БД`:
> 
> C - Create
> R - Read
> U - Update
> D - Delete

![[Pasted image 20241108182643.png]]

Обычно CRUD приложения работают с пользователями по сети, то есть являются также и Web-приложениями
![[Pasted image 20241108181322.png]]

---
# 2. Паттерн REST
REST (REpresentational State Transfer, передача состояния представления)

> [!NOTE] REST
> - REST - паттерн проектирования Web-приложений
> 	
> - REST описывает то, как посредством HTTP, клиент должен взаимодействовать с сервером
> 	  
> - Все действия с сервером сводятся к 4 действиям:
>     - Получение данных ( HTTP-метод GET)
> 	- Добавление новых данных (POST)
> 	- Изменение данных (PUTCH)
> 	- Удаление данных (DELETE)
> 	
> 
> Это не все правил и ограничения, есть множество других

> [!NOTE] Требования REST
> Всего в REST есть шесть требований к проектированию API. Пять из них обязательные, одно — опциональное:
>- Клиент-серверная модель (client-server model).
>- Отсутствие состояния (statelessness).
>	- На сервере не хранится никаких данных о прошлых взаимодействиях с клиентом — > каждый запрос должен содержать всю информацию для его обработки.
>- Кэширование (cacheability).
>	- Сохранение части данных у клиента или на промежуточных серверах.
>- Единообразие интерфейса (uniform interface).
>	- Ответ на запросы к новому ресурсу должен приходить в том же формате, что и к старым
>- Многоуровневая система (layered system).
>	- Несколько промежуточных узлов, выполняющих вспомогательные функции, — **прокси-серверы**.
>- Код по требованию (code on demand) — необязательно.

## 2.1 Действия по соответствующему запросу (REST)
![[Pasted image 20241108182610.png]]

> [!NOTE] Сущность Post
> Сущность `Post это сущность в БД`, отвечающий за что-угодно, в данном примере не вдаемся в подробности
> 
> Также в `URL-адресе` необходимо писать эту `сущность во множественном числе`
## 2.2 Связь CRUD и REST

> [!NOTE] CRUD
> ==CRUD== - 4 базовые `функции`, используемые `при работе с БД`
> 
> ==REST== - `паттерн` проектирования `web-приложений`.
> - Описывает как при помощи HTTP взаимодействовать с сервером для выполнения основных операций CRUD (создание/добавление, чтение, изменение, удаление данных)
> - Иногда, помимо акронима REST, можно встретить слово ==**RESTful**==. Это не термин, но в сообществе его применяют к веб-сервисам, которые соответствуют REST-архитектуре.

![[Pasted image 20241108190638.png]]

---
# 3. Паттерн DAO (Data Access Object)

Как мы ранее изучали, `Model` в паттерне `MVC` необходим для работы с данными, а именно с БД.

> [!NOTE] Паттерн DAO
> Однако прямое взаимодействие `Model` с БД - плохое решение
> 
> Паттерн DAO - паттерн проектирования приложения, при котором `ОТДЕЛЬНЫЙ класс` занимается `взаимодействием с БД` для конкретной сущности

В классах DAO обычно пишется SQL код для работы с БД приложения (чтение, запись, обновление, удаление)

> Также есть другой паттерн проектирования для работы с БД - `Repository`, реализующий большую абстракцию над БД, чем DAO



