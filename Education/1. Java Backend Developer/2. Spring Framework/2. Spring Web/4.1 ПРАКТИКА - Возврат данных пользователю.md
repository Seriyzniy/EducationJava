Будем использовать проект из предыдущей практики.

В этом конспекте создадим простой Web-калькулятор, который выполняет математические действия при помощи URL запроса
# Использование модели в методе
Самый простой способ передать данные — использовать параметр `Model` в методе контроллера.

```java
@GetMapping("/hello")
public String sayHello(HttpServletRequest request, Model myModel) {
```

Теперь объект `model` можно будет использовать для отправки данных на форму

# Создание калькулятора
С помощью класса `HttpServletRequest` возьмем данные отправленные пользователем через URL

Возможные параметры
`a` - первое число
`b` - второе число
`action` - мат. действие

```java
@GetMapping("/hello")

public String sayHello(HttpServletRequest request, Model myModel) {
	int a = Integer.parseInt(request.getParameter("a"));
	int b = Integer.parseInt(request.getParameter("b"));
	String action = request.getParameter("action");
	double result = 0;
	
	switch(action) {
		case "sum":
			result = a+b;
			break;
		case "minus":
			result = a-b;
			break;
		case "multi":
			result = a*b;
			break;
		case "devide":
			result = a/(double)b;
			break;
	}
	myModel.addAttribute("result", result);
	return "myfirst/hellopage";
}
```

