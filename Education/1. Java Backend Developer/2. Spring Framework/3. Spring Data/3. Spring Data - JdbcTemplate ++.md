Смотри подразделы:
- [[3.2 ПРАКТИКА - JdbcTemplate]]
***

JdbcTemplate - обертка вокруг JDBC API, ==предоставляемая Spring Framework==

> [!NOTE] `PreparedStatement` в `JdbcTemplate`
> `JdbcTemplate` ПО УМОЛЧАНИЮ использует `PreparedStatement`

# 1. Проблемы JDBC API

1) При использовании JDBC API мы пишем ==много лишнего кода== (создаем Connection, Statement, достаем данные из ResultSet и прочее)

![[Pasted image 20241125124322.png]]
***

2) ==Дублирование кода==

В первом случае представлен код для метода `getAllPerson()` а во втором `getPerson()`
![[Pasted image 20241125124516.png]]
***

3) Неинформативный ==SQL Exception==, который нужно обрабатывать

![[Pasted image 20241125124839.png]]

***
# 2. Использование JdbcTemplate вместо обычного JDBC API
![[Pasted image 20241125125133.png]]
