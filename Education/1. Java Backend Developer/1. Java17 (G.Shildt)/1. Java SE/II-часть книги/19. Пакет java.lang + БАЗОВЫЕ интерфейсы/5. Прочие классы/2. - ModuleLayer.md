- Отражает **уровень загрузки модулей** в JVM.
    
- Обычно используется в спец. сценариях (напр. контейнеры, динамическая подгрузка).
    
- Есть вложенный `ModuleLayer.Controller` для управления слоями.  
    Для повседневного программирования почти не нужен.