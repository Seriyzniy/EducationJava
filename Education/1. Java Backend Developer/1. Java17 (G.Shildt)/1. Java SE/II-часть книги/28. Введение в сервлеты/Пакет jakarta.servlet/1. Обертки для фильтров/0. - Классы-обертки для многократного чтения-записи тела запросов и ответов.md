В стандартной работе с сервлетами возникает фундаментальное ограничение: ==**ServletInputStream и ServletOutputStream позволяют прочитать или записать данные тела запроса/ответа ТОЛЬКО ОДИН РАЗ**. ==

Это связано с тем, что эти потоки работают непосредственно с сетевым соединением, и ==данные "уплывают" к клиенту или "проходят" от клиента== без возможности отката.

## Проблема однократного доступа:
```java
// ❌ НЕВОЗМОЖНО - прочитать тело запроса дважды
// ✅Данные прочитаны и удалены из потока
String body1 = readRequestBody(request); 

// ❌Пусто! Поток уже завершен
String body2 = readRequestBody(request);




// ❌ НЕВОЗМОЖНО - записать ответ, потом его модифицировать

response.getWriter().write("Hello"); // ✅Данные ушли клиенту
response.getWriter().write("World"); // ❌Добавляется, но нельзя отозвать первое
```

## Решение через обертки:
**Классы-обертки** решают эту проблему путем перехвата и буферизации данных:

### Для запросов:
```java
public class CachedBodyRequestWrapper extends HttpServletRequestWrapper {
    private final byte[] cachedBody;

    public CachedBodyRequestWrapper(HttpServletRequest request) throws IOException {
        super(request);
        // читаем и кешируем тело запроса
        cachedBody = request.getInputStream().readAllBytes();
    }

    @Override
    public ServletInputStream getInputStream() {
        // возвращаем новый поток поверх сохранённого тела
        return new CachedBodyInputStream(cachedBody);
    }

    // ⭐⭐кастомный ServletInputStream⭐⭐
    private static class CachedBodyInputStream extends ServletInputStream {
        private final ByteArrayInputStream inputStream;

        public CachedBodyInputStream(byte[] body) {
            this.inputStream = new ByteArrayInputStream(body);
        }

        @Override
        public int read() {
            return inputStream.read();
        }

        @Override
        public boolean isFinished() {
            return inputStream.available() == 0;
        }

        @Override
        public boolean isReady() {
            return true;
        }

        @Override
        public void setReadListener(ReadListener readListener) {}
    }
}
```

### Для ответов:
```java
public class CachedBodyResponseWrapper 
	extends HttpServletResponseWrapper 
{
    private final ByteArrayOutputStream buffer = 
	    new ByteArrayOutputStream();
	    
    private final ServletOutputStream outputStream = 
	    new CachedBodyOutputStream(buffer);
	    
    private PrintWriter writer = new PrintWriter(outputStream, true);



    public CachedBodyResponseWrapper(HttpServletResponse response) {
        super(response);
    }

    @Override
    public ServletOutputStream getOutputStream() {
	    // ⭐ Возвращаем КАСТОМНЫЙ ПОТОК
        return outputStream;
    }

    @Override
    public PrintWriter getWriter() {
        return writer;
    }

    public byte[] getCachedBody() {
        writer.flush();
        return buffer.toByteArray();
    }


    // ⭐ кастомный ServletOutputStream ⭐
    private static class CachedBodyOutputStream 
	    extends ServletOutputStream 
	{
        private final OutputStream stream;

        public CachedBodyOutputStream(OutputStream stream) {
            this.stream = stream;
        }

        @Override
        public void write(int b) throws IOException {
            stream.write(b);
        }

        @Override
        public boolean isReady() {
            return true;
        }

        @Override
        public void setWriteListener(WriteListener writeListener) {}
    }
}
```