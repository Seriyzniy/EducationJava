В своей простейшей форме перечисление ==представляет собой список именованных констант==, который определяет новый тип данных и ero допустимые значения.


> [!NOTE] ⭐Перечисления - класс
> Хотя на первый взгляд перечисления в Java могут показаться похожими на перечисления в других языках, это сходство окажется поверхностным, по скольку в Java перечисление определяется как тип класса. 
> 
> Скажем, в Java ==перечисление может иметь конструкторы, методы и переменные экземпляра==

# 📕 1. Основы перечислений
Перечисление ==создается== с применением ==ключевого слова enum==. 

Например, вот простое перечисление, в котором определен перечень различных сортов яблок
```java
// Перечисление сортов яблок. 
enum Apple { 
	Jonathan, GoldenDel, RedDel, Winesap, Cortland
}
```

Объявление переменной типа ПЕРЕЧИСЛЕНИЯ происходит БЕЗ УЧАСТИЯ `new()`
```java
Apple ap; //Объявление переменной типа Appale

ap = Apple.RedDel // Присвоение значения ТОЛЬКО из типа Apple

if(ap = Apple.Jonathan) //Можно сравнивать

switch(ap){ //Можно использовать в switch/case
	case Jonathan: {}
	case GoldenDel{}
	...
}
```

***
# 📕 2. Методы values () и valueOf ()

==Все перечисления автоматически содержат в себе два== предопределенных ==метода==: `values()` и `valueOf()`
```java
//Возвращает массив констатн перечисления
puЬlic static enurn-type[] values () 

//Возвращает константу перечисления, имя которой совпадает с параметром
puЬlic static enum-type valueOf (String str)
```

```java
enum Apple {
    Jonathan, GoldenDel, RedDel, Winesap, Cortland
}

class EnumDemo2 {
    public static void main(String[] args) {
        Apple ap;
        System.out.println("Все константы перечисления Apple:");
        
        // Использовать values()
        Apple[] allapples = Apple.values();
        for (Apple a : allapples)
            System.out.println(a);
        
        System.out.println();
        
        // Использовать valueOf()
        ap = Apple.valueOf("Winesap");
        System.out.println("ap содержит " + ap);
    }
}
Все константы перечисления Apple:
Jonathan 
GoldenDel
RedDel 
Winesap 
Cortland 

ар содержит Winesap
```

***
# 📕 3.Перечисления Java являются типами классов

Тот факт, что enum определяет класс, придает перечислению в Java необычайную силу. 

Например, вы ==можете предоставить ему КОНСТРУКТОРЫ, добавить ПЕРЕМЕННЫЕ экземпляра и МЕТОДЫ и даже РЕАЛИЗОВЫВАТЬ ИНТЕРФЕЙСЫ.==

> [!NOTE] ⭐Каждая константа имеет свою копию переменных объявленных в `enum`
> Кроме того, каждая константа перечисления имеет собственную копию любых переменных экземпляра, определенных перечислением
> 
> ⭐⭐Конструктор вызывается для каждой константы⭐⭐
> Когда переменная ар объявляется в main ( ) , ==конструктор Apple вызывается по одному разу для каждой== заданной ==константы==
```java
enum Apple {
    Jonathan(10), //У каждой константы своя копия переменной price
    GoldenDel(9),
    RedDel(12),
    Winesap(15),
    Cortland(8);
    
    private int price; // цена яблок каждого сорта
    
    // Конструктор
    Apple(int p) {
        price = p;
    }
    
    int getPrice() {
        return price;
    }
}

class EnumDemo3 {
    public static void main(String[] args) {
        Apple ap;
        
        // Отобразить цену яблок сорта Winesap
        System.out.println("Яблоки сорта Winesap стоят " +
                          Apple.Winesap.getPrice() +
                          " центов.\n");
        
        // Отобразить все сорта яблок вместе с ценами
        System.out.println("Цены на все сорта яблок:");
        for (Apple a : Apple.values())
            System.out.println("Яблоки сорта " + a + " стоят " +
                             a.getPrice() + " центов.");
    }
}
Яблоки сорта Winesap стоят 15 центов. 

Цены на все сорта яблок: 
Яблоки сорта Jonathan стоят 10 центов. 
Яблоки сорта GoldenDel стоят 9 центов. 
Яблоки сорта RedDel стоят 12 центов. 
Яблоки сорта Winesap стоят 15 центов. 
Яблоки сорта Cortland стоят 8 центов.
```

> [!NOTE] 🛑🛑🛑Важные 2 ОГРАНИЧЕНИЯ `enum` 🛑🛑🛑
> 1) Во-первых, перечисление ==НЕ может быть УНАСЛЕДОВАНО от другого класса==. 
> 2) Во-вторых, перечисление не может служить суперклассом, т.е. ==перечисление РАСШИРЯТЬ НЕЛЬЗЯ==. 
> 
> В остальном перечисление действует так же, как любой другой тип класса

***
# 📕 4. Перечисления унаследованы от `Enum`

Все перечисления автоматически унаследованы от `java.lang.Enum`. 
В этом классе определено несколько методов, доступных для использования всеми перечислениями.

```java
final int ordinal()
```
Метод `ordinal()` ==возвращает порядковый номер константы==, на которой вызывается. Порядковые номера начинаются с нуля. 
Таким образом, в пере числении Apple константа Jonathan имеет порядковый номер 0, константа GoldenDel - порядковый номер 1

```java
final int compareTo (enum-type е)
```
Порядковые номера двух констант одного и того же перечисления можно сравнивать с применением метода
Если `this < e` - отрицательное, иначе положительное число. Если равны - 0

