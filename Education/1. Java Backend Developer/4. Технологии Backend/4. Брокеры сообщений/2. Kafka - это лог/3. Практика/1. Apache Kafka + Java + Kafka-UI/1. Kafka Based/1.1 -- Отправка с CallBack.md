Изменим Producer'а, и укажем в методе send Callback
```java
public void produceMessaging(){  
    try(producer){  
        ProducerRecord<String, String> record =  
            new ProducerRecord<>(  
                "mytopic",  
                "Hello from Producer with CallBack");  
  
        producer.send(record, new Callback() {  
                    @Override  
                    public void onCompletion(
	                    RecordMetadata recordMetadata, 
	                    Exception e) 
	                {  
                        if(e == null){  
                            System.out.println(
	                            "Successfully sent message."+
	                            "Got ACK");  
                        }  
                        else{  
                            System.out.println(
	                            "Error sending message");  
                        }  
                    }  
                }  
        );  
        System.out.println("[INFO] Message sent");  
  
    } catch (Exception e){  
        System.out.println("Exception on produce message");  
    }  
}
```

## Запуск
### Запуск Producer
Теперь запустим Producer
![[Pasted image 20251116104754.png]]

Как видим, сообщение отправилось, НО ПОДТВЕРЖДЕНИЕ от Kafka мы получили чуть поже

### Kafka-UI
Посмотри что лежит в Kafka-UI в топике
![[Pasted image 20251116104921.png]]

### Запуск Cosnumer
![[Pasted image 20251116134230.png]]

Если позапускать несколько раз этого Consuemr
При этом, в логах кафки можно увидеть, что новый Consumer:
- обнаруживается и генерируется id для него
- добавляются в группу
- происходит ребалансировка
- проверка, что группа стабильна
- получение сообщения
![[Pasted image 20251116134438.png]]

