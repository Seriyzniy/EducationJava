Теперь, попробуем написать программу с использованием ActiveMQ.

Создадим простое Producer/Concumer приложение, которое будет отправлять сообщение через ActiveMQ.
ActiveMQ развернем в докере, а приложение будет разворачивать локально.

![[Pasted image 20251102104021.png]]

## Напоминание про JMS API
![[Pasted image 20251102084930.png]]


> [!NOTE] ⭐javax VS jakarta ⭐
> Некоторые версии ActiveMQ используют jakarta, вместо устаревшей javax
> ❗❗❗БУДЕМ ИСПОЛЬЗОВАТЬ jakarta❗❗❗


# 1. Создание проекта в IntelijIDEA и подключение JMS API
Теперь создадим проект в IntelijIDEA (можно и в Eclipse).

Это обычный проект Java. После создание проекта, добавим два пакета producer, consumer
![[Pasted image 20251102104237.png]]

## Зависимость JMS API
Так как ранее упоминалось, что ActiveMQ, использует спецификацию JMS (Java Message Service), то нам необходимо подключить эту зависимость (в виде jar файла)

Для этого перейдем на Maven Repository, и найдем там JMS API. И скачаем оттуда архив jar
![[Pasted image 20251102123826.png]]

Скаченный архив jar необходимо подключить к созданному проекту, для этого в проекте в IntelijIDEA:
`File->Project Structure->Modules->Dependencies`
![[Pasted image 20251102124057.png]]

Затем кнопку Apply.

## Зависимость ActiveMQ

Так как мы собираемся использовать ActiveMQ, то подключим и его аналогичным образом
![[Pasted image 20251102124221.png]]

> Если нажать на `View all`, то можно увидеть все файлы. Там есть файл `activemq-core.jar`

Подключаем к проекту аналогичным образом как и подключали JMS

## ⭐Зависимость Log4j для ActiveMQ ⭐
Если попытаться запустить программу с этими зависимостями, то будет ошибка:
![[Pasted image 20251102165353.png]]

```java
D:\Education_Java\JavaProgramming\ToolsAndConfig\Java\jdk17\bin\java.exe "-javaagent:D:\Education_Java\JavaProgramming\Java_IDE\InteliJIDEA\Intelij_Ultimate\IntelliJ IDEA 2024.3\lib\idea_rt.jar=51930:D:\Education_Java\JavaProgramming\Java_IDE\InteliJIDEA\Intelij_Ultimate\IntelliJ IDEA 2024.3\bin" -Dfile.encoding=UTF-8 -classpath D:\Education_Java\JavaProgramming\MyProjects\IntelijIDEA\projects\ActiveMqExample\out\production\ActiveMqExample;C:\Users\sasam\Downloads\jakarta.jms-api-3.1.0.jar;C:\Users\sasam\Downloads\activemq-all-6.1.8.jar producer.ActiveMqProducer
Exception in thread "main" java.lang.NoClassDefFoundError: org/apache/logging/log4j/Logger
	at org.apache.logging.slf4j.SLF4JServiceProvider.initialize(SLF4JServiceProvider.java:56)
	at org.slf4j.LoggerFactory.bind(LoggerFactory.java:201)
	at org.slf4j.LoggerFactory.performInitialization(LoggerFactory.java:187)
	at org.slf4j.LoggerFactory.getProvider(LoggerFactory.java:511)
	at org.slf4j.LoggerFactory.getILoggerFactory(LoggerFactory.java:497)
	at org.slf4j.LoggerFactory.getLogger(LoggerFactory.java:446)
	at org.slf4j.LoggerFactory.getLogger(LoggerFactory.java:472)
	at org.apache.activemq.ActiveMQConnectionFactory.<clinit>(ActiveMQConnectionFactory.java:63)
	at producer.ActiveMqProducer.<clinit>(ActiveMqProducer.java:9)
Caused by: java.lang.ClassNotFoundException: org.apache.logging.log4j.Logger
	at java.base/jdk.internal.loader.BuiltinClassLoader.loadClass(BuiltinClassLoader.java:641)
	at java.base/jdk.internal.loader.ClassLoaders$AppClassLoader.loadClass(ClassLoaders.java:188)
	at java.base/java.lang.ClassLoader.loadClass(ClassLoader.java:525)
	... 9 more
```

Ошибка связана с тем, что ActiveMQ под капотом использует Log3j из пакета org/apache/logging/log4j/Logger

Чтобы исправить ошибку, необходимо добавить jar файл с API и Core.

> 💡Подсказка💡:
> В ошибке указан какой именно класс используется и из какого пакета. Поэтому нужно найти именно этот пакет в зависимости
> К тому же известно, что класс из пакета `org.apache`

![[Pasted image 20251102165851.png]]

Однако одной зависимости не хватило, и методом тыка (выбрал более общее) нашел еще один jar файл
![[Pasted image 20251102165933.png]]

Эти файлы jar необходимо подключить к проекты аналогичным образом

---
# 2. Развертывание ActiveMQ в Docker

Теперь, создадим сам ActiveMQ в докере. Для этого в DockerHub найдем образ ActiveMq

![[Pasted image 20251102112331.png]]

Выберем этот образ, поскольку тут есть веб-интерфейс для ActiveMq `(3)`.

Загрузим его, и при старте укажем порты на хосте такие же, как и в образе (61616 для протокола OpenWire, и 8161 для веб-интерфейса, другие протоколы можно использовать - НО С ДРУГИМИ классами, а не ActiveMqConnectionFactory)

> Как указано в описании образа, для подключения к веб-интерфейсу можно использовать
> login: admin или someone
> pass: password

![[Pasted image 20251102115429.png]]

---
# 3. Описание общих объектов для Producer и Consumer
## Определение ConnectionFactory
Для начала, нам необходимо создать `ConnectionFactory`, для того чтобы определить создание подключения `Connection`.

Так как мы уже знаем, что ActiveMQ реализует спецификацию JMS, то у него есть своя реализация интерфейса `ConnectionFactory`

У этой реализации (ActiveMQConnectionFactory) есть следующие конструкторы
```java
public ActiveMQConnectionFactory() {  
    this("failover://tcp://localhost:61616");  
}  
  
public ActiveMQConnectionFactory(String brokerURL) {  
}  
  
public ActiveMQConnectionFactory(URI brokerURL) {  
     
}  
  
public ActiveMQConnectionFactory(
	String userName, 
	String password, 
	URI brokerURL) {  
      
}  
  
public ActiveMQConnectionFactory(
	String userName, 
	String password, 
	String brokerURL) {  
    
```

Как видно, есть конструктор по умолчанию, который использует протокол OpenWire (порт 61616 - видно из образа в Docker).

Но мы будем использовать MQTT, который поддерживает модель взаимодействия "публикация-подписка".

Соответственно, наш параметр конструктора Фабрики для подключении будет:
```java
"tcp://localhost:61616"
//❗❗см как строится URL в Java/2 часть книги/Работа в сети❗❗
```

## Определение Connection
Теперь, когда у нас будет фабрика, мы можем создать объект `Connection`.

Посмотрим в документации ([Connection (Java(TM) EE 7 Specification APIs)](https://docs.oracle.com/javaee/7/api/javax/jms/Connection.html)) какие методы у него есть, и что они делают:

```java
//Выполняет подключение для получения/отправки сообщений
void start()

//Создает сессию (можно указть только мод)
Session createSession(boolean transacted, int acknowledgeMode)

//Выполняет отключение получения/отправки сообщений
void stod()
```

Примерно в таком порядке и будут вызываться методы.

## Определение Session
Теперь, когда у нас есть сессия, мы можем создать MessageProducer и Message

Опять заглянем в документацию JMS API ([Session (Java(TM) EE 7 Specification APIs)](https://docs.oracle.com/javaee/7/api/javax/jms/Session.html))

```java
//Создает объект сообщения Message
Message createMessage()

//Создает ТЕКСТОВОЕ сообщенеи
TextMessage createTextMessage(String text)

//Создает ОЧЕРЕДЬ
//❗❗Queue - очередь ИЗ ПАКЕТА javax.jms❗❗
Queue createQueue(String queueName)

//Создает ТОПИК
Topic createTopic(String topicName)

//Создает продюсера с определнынм адресатом (топик или очередь)
MessageProducer createProducer(Destination destination)

//Создает консьюмера с определнынм адресатом (топик или очередь)
//💡ТАК же есть варианты создания консьюмера в виде ПОДПИСЧИКА💡
MessageConsumer createConsumer(Destination destination)
```

> **Destitation** является корневым интерфейсом для классов из пакета javax.jms, а именно для:
> - Queue
> - Topic

---
# 📕 1. Описание Producer
Посмотрим документацию MessageProducer ([MessageProducer (Java(TM) EE 7 Specification APIs)](https://docs.oracle.com/javaee/7/api/javax/jms/MessageProducer.html))

Одним из главных методов, является send()
```java
void send(Message message)
```

Теперь опишем свой класс Producer
```java
package producer;  
  
import jakarta.jms.*;  
import org.apache.activemq.ActiveMQConnectionFactory;  
  
import java.util.Scanner;  
  
public class ActiveMqProducer {  
    private static final ConnectionFactory connectionFactory =  
            new ActiveMQConnectionFactory(
	            "tcp://localhost:61616"
	        );  
  
    private static final Scanner scanner = new Scanner(System.in);  
  
    public static void main(String[] args) {  
        String input = " ";  
        try {  
            System.out.println("[INFO] Create connection");  
            Connection connection = 
	            connectionFactory.createConnection();  
            connection.start();  
  
            System.out.println("[INFO] Create session");  
            Session session = 
	            connection.createSession(
		            false, 
		            Session.AUTO_ACKNOWLEDGE
		        );  
  
            System.out.println("[INFO] Create producer");  
            MessageProducer producer = 
	            session.createProducer(
		            session.createQueue("MessageQueue")
		        );  
  
            System.out.println("[INFO] Create text message");  
            TextMessage textMessage = 
	            session.createTextMessage();  
  
            System.out.println(
	            "[INFO] Input text (enter q to exit)"
	        );  
            while(!input.equals("q")){  
                System.out.print(">");  
                input = scanner.nextLine();  
                textMessage.setText(input);  
  
                System.out.println("[INFO] Message sent");  
                producer.send(textMessage);  
            }  
  
            System.out.println("[INFO] All complete");  
  
            session.close();  
            connection.stop();  
        } catch (JMSException e) {  
            System.out.println("[Exception] Error to create connection");  
        }  
    }  
}
```

## Микро-тест
Теперь создадим контейнер, и укажем порты:
- 61616 - для протокола подключения OpenWire
- 8161 - для веб-интерфейса

![[Pasted image 20251102172009.png]]

Теперь перейдем в веб-интерфейс на `localhost:8161`
![[Pasted image 20251102172105.png]]

Теперь запустим программу
![[Pasted image 20251102174900.png]]

Сразу после запуска создается очередь MessageQueue, и в нем появляется сообщение
![[Pasted image 20251102174935.png]]

![[Pasted image 20251102174948.png]]

---
# 📕 2. Описание Consumer
Теперь опишем консюмера. Но перед началом посмотрим в документацию [MessageConsumer (Java(TM) EE 7 Specification APIs)](https://docs.oracle.com/javaee/7/api/javax/jms/MessageConsumer.html)

Выбор методов не богат, и основным является метод 
```java
Message receive()
```
для получения сообщения

Опишем класс Consumer
```java
package consumer;

import jakarta.jms.*;
import org.apache.activemq.ActiveMQConnectionFactory;

public class ActiveMqConsumer {
    private static final ConnectionFactory connectionFactory =
            new ActiveMQConnectionFactory("tcp://localhost:61616");

    public static void main(String[] args) {
        try {
            System.out.println("[INFO] Create connection");
            Connection connection = 
	            connectionFactory.createConnection();
            connection.start();

            System.out.println("[INFO] Create session");
            Session session = 
	            connection.createSession(
		            false, 
		            Session.AUTO_ACKNOWLEDGE
		        );

            System.out.println("[INFO] Create producer");
            MessageConsumer consumer = 
	            session.createConsumer(
		            session.createQueue("MessageQueue")
		        );

            System.out.println("[INFO] Receive message");
            Message receiveMessage = consumer.receive();

            System.out.println(
	            "Receive message:" +
	            receiveMessage.getBody(String.class)
	        );

            System.out.println("[INFO] All complete");

            session.close();
            connection.stop();
        } catch (JMSException e) {
            System.out.println("[Exception] Error to create connection");
        }
    }
}
```

Теперь запустим программу:
![[Pasted image 20251102175712.png]]

Как видно, сообщение получено

И из очереди сообщение пропало (так как мы использовали очередь, а не топик, из-за чего сообщенеи досталось только одному консьюмеру)
![[Pasted image 20251102175750.png]]








