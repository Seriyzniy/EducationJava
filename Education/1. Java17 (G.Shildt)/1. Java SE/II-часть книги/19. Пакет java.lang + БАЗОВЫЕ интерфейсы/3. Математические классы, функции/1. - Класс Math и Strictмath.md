Класс `Маth` содержит ==методы для всех функций с плавающей точкой==, которые применяются в геометрии и тригонометрии, а также несколько методов общего назначения. 

В Math определены две константы типа douЫe: Е (приблизительно 2,72) и PI (приблизительно 3,14).

# 📕 1. Тригонометрические функции

| Метод                                     | Описание                                                                  |
| ----------------------------------------- | ------------------------------------------------------------------------- |
| **Основные тригонометрические функции**   |                                                                           |
| `static double sin(double arg)`           | Возвращает **синус** угла, указанного с помощью `arg` в **радианах**      |
| `static double cos(double arg)`           | Возвращает **косинус** угла, указанного с помощью `arg` в **радианах**    |
| `static double tan(double arg)`           | Возвращает **тангенс** угла, указанного с помощью `arg` в **радианах**    |
|                                           |                                                                           |
| **Обратные тригонометрические функции**   |                                                                           |
| `static double asin(double arg)`          | Возвращает **арксинус** (угол в радианах), синус которого равен `arg`     |
| `static double acos(double arg)`          | Возвращает **арккосинус** (угол в радианах), косинус которого равен `arg` |
| `static double atan(double arg)`          | Возвращает **арктангенс** (угол в радианах), тангенс которого равен `arg` |
| `static double atan2(double y, double x)` | Возвращает **арктангенс** отношения `y/x` с учетом квадранта              |
|                                           |                                                                           |
| **Гиперболические функции**               |                                                                           |
| `static double sinh(double arg)`          | Возвращает **гиперболический синус** угла `arg`                           |
| `static double cosh(double arg)`          | Возвращает **гиперболический косинус** угла `arg`                         |
| `static double tanh(double arg)`          | Возвращает **гиперболический тангенс** угла `arg`                         |
# 📕 2. Экспоненциальные методы
| Метод                                         | Описание                                                            |
| --------------------------------------------- | ------------------------------------------------------------------- |
| **Степенные и корневые функции**              |                                                                     |
| `static double pow(double y, double x)`       | Возвращает `y` в степени `x`; например, `pow(2.0, 3.0)` дает `8.0`  |
| `static double sqrt(double arg)`              | Возвращает **квадратный корень** значения `arg`                     |
| `static double cbrt(double arg)`              | Возвращает **кубический корень** значения `arg`                     |
| `static double scalb(double arg, int factor)` | Возвращает `arg × 2^factor`                                         |
| `static float scalb(float arg, int factor)`   | Возвращает `arg × 2^factor`                                         |
|                                               |                                                                     |
| **Экспоненциальные функции**                  |                                                                     |
| `static double exp(double arg)`               | Возвращает **e** в степени `arg`                                    |
| `static double expm1(double arg)`             | Возвращает **e** в степени `arg-1`                                  |
|                                               |                                                                     |
| **Логарифмические функции**                   |                                                                     |
| `static double log(double arg)`               | Возвращает **натуральный логарифм** (по основанию e) значения `arg` |
| `static double log10(double arg)`             | Возвращает **логарифм по основанию 10** значения `arg`              |
| `static double log1p(double arg)`             | Возвращает **натуральный логарифм** значения `arg+1`                |

# 📕 3. Методы округления

| Метод                                                | Описание                                                                                                                          |
| ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| **Абсолютные значения**                              |                                                                                                                                   |
| `static int abs(int arg)`                            | Возвращает **абсолютное значение** `arg`                                                                                          |
| `static long abs(long arg)`                          | Возвращает **абсолютное значение** `arg`                                                                                          |
| `static float abs(float arg)`                        | Возвращает **абсолютное значение** `arg`                                                                                          |
| `static double abs(double arg)`                      | Возвращает **абсолютное значение** `arg`                                                                                          |
|                                                      |                                                                                                                                   |
| **Округление вверх/вниз**                            |                                                                                                                                   |
| `static double ceil(double arg)`                     | Возвращает **наименьшее целое число**, которое **больше или равно** `arg`                                                         |
| `static double floor(double arg)`                    | Возвращает **наибольшее целое число**, которое **меньше или равно** `arg`                                                         |
| `static double rint(double arg)`                     | Возвращает **целое число, ближайшее** по значению к `arg`                                                                         |
|                                                      |                                                                                                                                   |
| **Целочисленное деление**                            |                                                                                                                                   |
| `static int floorDiv(int dividend, int divisor)`     | Возвращает **округленный в меньшую сторону** результат деления `dividend/divisor`                                                 |
| `static long floorDiv(long dividend, int divisor)`   | Возвращает **округленный в меньшую сторону** результат деления `dividend/divisor`                                                 |
| `static long floorDiv(long dividend, long divisor)`  | Возвращает **округленный в меньшую сторону** результат деления `dividend/divisor`                                                 |
| `static int floorMod(int dividend, int divisor)`     | Возвращает **округленный в меньшую сторону** остаток от деления `dividend/divisor`                                                |
| `static int floorMod(long dividend, int divisor)`    | Возвращает **округленный в меньшую сторону** остаток от деления `dividend/divisor`                                                |
| `static long floorMod(long dividend, long divisor)`  | Возвращает **округленный в меньшую сторону** остаток от деления `dividend/divisor`                                                |
|                                                      |                                                                                                                                   |
| **Минимум/Максимум**                                 |                                                                                                                                   |
| `static int max(int x, int y)`                       | Возвращает **большее** значение среди `x` и `y`                                                                                   |
| `static long max(long x, long y)`                    | Возвращает **большее** значение среди `x` и `y`                                                                                   |
| `static float max(float x, float y)`                 | Возвращает **большее** значение среди `x` и `y`                                                                                   |
| `static double max(double x, double y)`              | Возвращает **большее** значение среди `x` и `y`                                                                                   |
| `static int min(int x, int y)`                       | Возвращает **меньшее** значение среди `x` и `y`                                                                                   |
| `static long min(long x, long y)`                    | Возвращает **меньшее** значение среди `x` и `y`                                                                                   |
| `static float min(float x, float y)`                 | Возвращает **меньшее** значение среди `x` и `y`                                                                                   |
| `static double min(double x, double y)`              | Возвращает **меньшее** значение среди `x` и `y`                                                                                   |
|                                                      |                                                                                                                                   |
| **Следующие числа**                                  |                                                                                                                                   |
| `static double nextAfter(double arg, double toward)` | Начиная со значения `arg`, возвращает следующее значение в направлении после `toward`. Если значения равны, возвращается `toward` |
| `static float nextAfter(float arg, double toward)`   | Начиная со значения `arg`, возвращает следующее значение в направлении после `toward`. Если значения равны, возвращается `toward` |
| `static double nextUp(double arg)`                   | Возвращает следующее значение в **положительном** направлении от `arg`                                                            |
| `static float nextUp(float arg)`                     | Возвращает следующее значение в **положительном** направлении от `arg`                                                            |
| `static double nextDown(double val)`                 | Возвращает следующее значение, которое **меньше** `val`                                                                           |
| `static float nextDown(float val)`                   | Возвращает следующее значение, которое **меньше** `val`                                                                           |
|                                                      |                                                                                                                                   |
| **Округление**                                       |                                                                                                                                   |
| `static int round(float arg)`                        | Возвращает результат **округления** `arg` до ближайшего числа типа `int`                                                          |
| `static long round(double arg)`                      | Возвращает результат **округления** `arg` до ближайшего числа типа `long`                                                         |
|                                                      |                                                                                                                                   |
| **Единицы измерения**                                |                                                                                                                                   |
| `static float ulp(float arg)`                        | Возвращает количество **единиц на последнем месте** (ULP) для `arg`                                                               |
| `static double ulp(double arg)`                      | Возвращает количество **единиц на последнем месте** (ULP) для `arg`                                                               |

# 📕 4. Прочие методы

| Метод                                                          | Описание                                                                                                 |
| -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| **Точные арифметические операции**                             |                                                                                                          |
| `static int addExact(int arg1, int arg2)`                      | Возвращает результат **сложения** `arg1` и `arg2`. <br>При переполнении генерирует `ArithmeticException` |
| `static long addExact(long arg1, long arg2)`                   | Возвращает результат **сложения** `arg1` и `arg2`. <br>При переполнении генерирует `ArithmeticException` |
| `static int subtractExact(int arg1, int arg2)`                 | Возвращает результат **вычитания** `arg1-arg2`. <br>При переполнении генерирует `ArithmeticException`    |
| `static long subtractExact(long arg1, long arg2)`              | Возвращает результат **вычитания** `arg1-arg2`. <br>При переполнении генерирует `ArithmeticException`    |
| `static int multiplyExact(int arg1, int arg2)`                 | Возвращает результат **умножения** `arg1*arg2`. <br>При переполнении генерирует `ArithmeticException`    |
| `static long multiplyExact(long arg1, int arg2)`               | Возвращает результат **умножения** `arg1*arg2`. <br>При переполнении генерирует `ArithmeticException`    |
| `static long multiplyExact(long arg1, long arg2)`              | Возвращает результат **умножения** `arg1*arg2`. <br>При переполнении генерирует `ArithmeticException`    |
| `static int incrementExact(int arg)`                           | Возвращает `arg+1`. <br>При переполнении генерирует `ArithmeticException`                                |
| `static long incrementExact(long arg)`                         | Возвращает `arg+1`. <br>При переполнении генерирует `ArithmeticException`                                |
| `static int decrementExact(int arg)`                           | Возвращает `arg-1`. <br>При переполнении генерирует `ArithmeticException`                                |
| `static long decrementExact(long arg)`                         | Возвращает `arg-1`. <br>При переполнении генерирует `ArithmeticException`                                |
| `static int negateExact(int arg)`                              | Возвращает `-arg`. <br>При переполнении генерирует `ArithmeticException`                                 |
| `static long negateExact(long arg)`                            | Возвращает `-arg`. <br>При переполнении генерирует `ArithmeticException`                                 |
| `static int absExact(int arg)`                                 | Возвращает **абсолютное значение** `arg`                                                                 |
| `static long absExact(long arg)`                               | Возвращает **абсолютное значение** `arg`                                                                 |
|                                                                |                                                                                                          |
| **Специальные математические операции**                        |                                                                                                          |
| `static double fma(double arg1, double arg2, double arg3)`     | **Комбинированное умножение и сложение**: <br>добавляет `arg3` к произведению `arg1` и `arg2`            |
| `static float fma(float arg1, float arg2, float arg3)`         | **Комбинированное умножение и сложение**: <br>добавляет `arg3` к произведению `arg1` и `arg2`            |
| `static double hypot(double side1, double side2)`              | Возвращает **длину гипотенузы** прямоугольного<br>треугольника по длине двух катетов                     |
| `static double IEEEremainder(double dividend, double divisor)` | Возвращает **остаток** от деления `dividend/divisor` <br>по стандарту IEEE 754                           |
|                                                                |                                                                                                          |
| **Битовые операции**                                           |                                                                                                          |
| `static int getExponent(double arg)`                           | Возвращает **показатель степени 2**, <br>используемый двоичным представлением `arg`                      |
| `static int getExponent(float arg)`                            | Возвращает **показатель степени 2**, <br>используемый двоичным представлением `arg`                      |
| `static long multiplyFull(int arg1, int arg2)`                 | Возвращает результат `arg1*arg2` <br>как величину типа `long`                                            |
| `static long multiplyHigh(long arg1, long arg2)`               | Возвращает значение типа `long`, <br>содержащее **старшие биты** произведения `arg1*arg2`                |
|                                                                |                                                                                                          |
| **Знаки и преобразования**                                     |                                                                                                          |
| `static double copySign(double arg, double signarg)`           | Возвращает значение `arg` <br>с таким же **знаком**, как у `signarg`                                     |
| `static float copySign(float arg, float signarg)`              | Возвращает значение `arg` <br>с таким же **знаком**, как у `signarg`                                     |
| `static double signum(double arg)`                             | Определяет **знак** значения: `0` <br>если `arg=0`, `1` <br>если `arg>0`, `-1` если `arg<0`              |
| `static float signum(float arg)`                               | Определяет **знак** значения: `0` <br>если `arg=0`, `1` <br>если `arg>0`, `-1` если `arg<0`              |
| `static int toIntExact(long arg)`                              | Возвращает значение `arg` как `int`. <br>При переполнении генерирует `ArithmeticException`               |
|                                                                |                                                                                                          |
| **Преобразования углов**                                       |                                                                                                          |
| `static double toDegrees(double angle)`                        | Преобразует **радианы в градусы**                                                                        |
| `static double toRadians(double angle)`                        | Преобразует **градусы в радианы**                                                                        |
|                                                                |                                                                                                          |
| **Случайные числа**                                            |                                                                                                          |
| `static double random()`                                       | Возвращает **псевдослучайное число** между `0.0` и `1.0`                                                 |
***
# 📕 5. Класс Strictмath 
В классе `StrictMath` определен полный набор математических методов, которые аналогичны методам класса Math. 

Разница в том, что методы из `Strictмath` ==гарантированно генерируют идентичные результаты во всех реализациях Java==, тогда как методы из Math имеют большую свободу действий в целях повышения производительности. Важно отметить, что после выхода версии JDK 17 все математические вычисления выполняются строго.